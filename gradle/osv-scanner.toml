# OSV scans repository lockfiles. The shared scanner excludes
# gradle/verification-metadata.xml because it describes build-tool artifacts,
# so the former metadata-only package overrides are no longer needed.

[[IgnoredVulns]]
id = "GHSA-5gvw-p9qm-jgwh"
ignoreUntil = 2026-10-31
reason = "jackson-databind 2.22.0 is locked only for the root buildscript toolchain and is absent from application runtime configurations."

[[IgnoredVulns]]
id = "GHSA-5jmj-h7xm-6q6v"
ignoreUntil = 2026-10-31
reason = "jackson-databind 2.22.0 is locked only for the root buildscript toolchain and is absent from application runtime configurations."
