# dBcheck Current Checkout — Evidence-Driven Code Review Questions

## A. Review scope, change inventory, and source-of-truth discipline

### Q001 — Current review boundary

Before reviewing behavior, establish the exact boundary of the current checkout: record the repository or worktree, branch, `HEAD`, working-tree status, and any comparison base or target commit supplied by the user or repository task. If no base is supplied, do not invent one or reuse the historical UI-restructure hashes; review the current implementation and identify uncommitted changes separately. When a diff exists, inventory changed production code, tests, resources, Room schemas, generated artifacts, workflows, and documentation, and classify suspicious files from actual diff content rather than filenames, old file counts, or task reports. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q002 — Current architecture boundaries

Does the current checkout honor the architecture contracts documented in `PROJECT.md`: `domain/**` does not import `data`, `service`, `sync`, `ui`, `billing`, or `widget`; UI, service, and widget code do not consume Room entities directly; repositories and service ports map persistence or Android models to domain, report, or UI-facing models; `SessionReportCalculator` remains the shared report-data source; and Room writes plus session completion remain owned by `SessionRepository` and `AudioSessionManager` rather than UI code? Inspect the current locations of `AudioEngine`, `ToneGenerator`, `MediaPipeSoundClassifier`, `AudioInputDeviceRouter`, `MeasurementDatabaseGate`, and pure domain audio policies instead of assuming their former packages. Report only an actual dependency or ownership violation. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q003 — No accidental feature deletion

Can every user-facing feature affected by the navigation and Settings ownership changes still be traced through its current route or entry point, callback, ViewModel or controller, execution gate, repository or service, persistence or export side effect, and return path? Cover Hearing Test, Hearing Recovery, Tinnitus Pitch, Ambient Sounds, Sleep Monitor from both Meter and Hearing when enabled, Voice Baseline, calibration and input selection, exposure and peak alerts, audible alarm, spoken TTS risk prompts, passive monitoring, Health Connect, approximate session location, CSV/PDF/PNG/WAV paths, backup and restore, Clear history, lockscreen privacy, display toggles, Pro purchase, Camera Overlay, and About. Report only a feature that is demonstrably unreachable or disconnected. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q004 — Duplicate implementation check

Did moving cards and page content create duplicate business logic, duplicate preference writes, duplicate route constants, or duplicate hearing-health calculations instead of one source of truth? Inspect moved component packages, `AnalyticsViewModel`, `HearingViewModel`, `SettingsPages.kt`, and their former locations; do not label intentionally separate presentation mappings as duplication without proving equivalent repeated behavior. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q005 — Dead code and stale callers

After the UI ownership changes and later runtime refactors, are there stale imports, obsolete route callers, unused state fields, orphaned tests, previews, or resources, duplicate old files, or references to superseded implementations such as the monolithic `SettingsScreen`, Analytics-owned hearing tools, legacy `TfliteSoundClassifier`, old `domain` locations for `AudioEngine` or `ToneGenerator`, Room-v12-only assumptions, or pre-v13 export fields? Use repository-wide symbol searches and compiler evidence; absence of a textual reference is not by itself proof that a resource is safely removable. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q006 — Documentation hierarchy

Where current `PROJECT.md`, `AGENTS.md`, `STATUS.md`, `memory/MEMORY.md`, design references, release QA documents, and older restructure or product specifications differ, do they follow the documented hierarchy: live production code and build configuration first, current tests, schemas, and generated artifacts second, `PROJECT.md` third, and historical plans last? Verify current facts such as the five destinations, the internal `analytics` route, the graph-scoped Settings ViewModel, Room v13, MediaPipe Tasks Audio, target SDK 37, and 98 screenshot baselines. Identify only contradictions that could mislead implementation, review, or release work. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q007 — Generated artifact legitimacy

Are changes in `app/stability/app-debug.stability`, `app/stability/app-release.stability`, the 98 screenshot reference PNGs, exported Room schemas through version 13, native-library compatibility outputs, dependency locks and verification metadata, and release QA artifacts explained by corresponding source or configuration changes, or do they baseline an unintended stability, rendering, schema, supply-chain, or native-packaging regression? Compare the generating source and validation task before claiming that a generated diff is defective. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q008 — Reviewability of task and QA reports

Do `.superpowers` task reports and release QA documents accurately record the work and verification without being treated as production sources of truth, overstating unrun tests, or introducing repository noise contrary to project conventions? Distinguish source-count inventories and dated local report snapshots from executed current-HEAD evidence. A finding requires a concrete maintenance, disclosure, or correctness impact, not a preference about report files. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## B. Top-level navigation, adaptive shell, and back-stack behavior

### Q009 — Five-destination order

Does `BottomNavDestination.entries` in `BottomNavDestination.kt` define exactly Meter, Analytics-as-Trends, Hearing, History, and Settings in that visible order, and do both `BottomNavBar` and `DbCheckNavigationRail` consume the enum order without maintaining a second divergent list? Verify labels and icons in both compact and expanded-width shells. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q010 — Analytics route compatibility versus visible Trends label

Does `Screen.Analytics.route == "analytics"` remain stable for saved navigation state and internal compatibility while every user-visible navigation label, heading, content description, screenshot, and live document says “Trends” where intended? Do not report internal class/package names as user-facing defects unless they leak into UI or external contracts. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q011 — Hearing route registration

Is `Screen.Hearing` registered exactly once as a top-level `composable`, connected to `HearingScreenActions`, selectable from bar and rail, and restorable after switching tabs? Trace each action from the nav graph to its intended non-top-level destination and upgrade route. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q012 — Settings graph destination identity

Does the `navigation(startDestination = Screen.Settings.HOME_ROUTE, route = Screen.Settings.route)` graph work correctly with `BottomNavDestination.SETTINGS.screen.route == "settings"`, given that `settings` is a graph route rather than a leaf composable? Verify first selection, reselection, restoration, deep links or direct navigations, and selected-state calculation. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q013 — Selected top-level route mapping

Does `selectedTopLevelRouteFor` correctly map `history/detail/{sessionId}` and concrete detail routes to History, all current `settings/...` pages and the legacy query route to Settings, and only the actual Hearing root to Hearing? Look for false positives from `startsWith`, such as an unrelated route sharing a prefix, and prove whether such a route can exist. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q014 — Fullscreen navigation visibility

Do Hearing Test setup/active/results, Recovery setup/active, Tinnitus Pitch, Ambient playback, Sleep setup, and Camera Overlay all return `null` from `selectedTopLevelRouteFor`, thereby hiding bar and rail, while Session Detail intentionally stays within the History shell? Validate actual route strings and rendered shell behavior, not only policy unit tests. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q015 — Reselect-to-root semantics

Does `topLevelNavigationPolicy` reset a nested Settings page to `settings/home` when Settings is reselected, leave an already-rooted destination unchanged through `isAlreadyAtRoot`, and avoid duplicate graph entries? Exercise Settings home, each Settings child, History detail, and every ordinary root separately. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q016 — History detail reselection

When the current route is Session Detail and the user reselects History, does `topLevelNavigationPolicy` navigate to the History root with the intended state behavior, rather than treating the detail route as already at root or restoring the detail immediately? Confirm against a real back-stack sequence and `NavigationRoutePolicyTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q017 — Cross-stack state restoration

Does the combination of `popUpTo(findStartDestination().id) { saveState = policy.shouldRestoreState }`, `launchSingleTop`, and `restoreState = policy.shouldRestoreState` preserve scroll and nested state when switching between different top-level stacks without reviving stale nested destinations on reselection? Check Meter → Settings child → Hearing → Settings and History detail → Meter → History sequences. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q018 — Same-stack navigation state policy

Is `shouldRestoreState = !isSameTopLevelStack` correct for all callers of the shared `navigateTo` lambda, especially navigation to Settings from a Settings child and navigation targets that are not members of `BottomNavDestination`? Prove a defect with an actual caller and failing sequence rather than theorizing about an impossible target. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q019 — Legacy Settings redirect with `showPro=false`

Does navigating to `settings?showPro=false` remove the legacy entry inclusively and leave a clean `settings/home` back stack, including process-restored argument handling and repeated invocations? Verify `settingsLegacyRedirectPlan(false)`, the `LaunchedEffect`, and whether the unused `backTargetRoute` field indicates a real behavioral omission or merely harmless data. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q020 — Legacy Settings redirect with `showPro=true`

Does `settings?showPro=true` reliably create the intended Home → Pro & About stack so Back returns to Settings Home, with no duplicate Home or Pro entries when invoked from Meter, another Settings page, or an existing Settings stack? Compare `navigateToUpgrade` and the legacy composable redirect because they implement similar two-step navigation. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q021 — Direct `createRoute` contract

Do all callers correctly understand that `Screen.Settings.createRoute(false)` returns `settings/home` and `createRoute(true)` returns `settings/pro_about`, while `ROUTE_WITH_ARGS` exists only for legacy compatibility? Search all callers for assumptions that `createRoute` still builds a query string or that Back from direct Pro navigation must always land on Home. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q022 — Hearing-flow return destinations

After moving hearing ownership out of Analytics, do successful completion, cancellation, system Back, and explicit Back actions from Hearing Test, Recovery, Tinnitus, Ambient, and Sleep flows return to the intended Hearing hub rather than Analytics, Meter, or an empty stack? Inspect every `popBackStack`, `navigate`, and completion callback in `DbCheckNavHost.kt` and the nested screens. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q023 — Pro navigation gates after route movement

Do route-level gates for Tinnitus Pitch, Ambient Sounds, Hearing Recovery, Camera Overlay, and Sleep execute before protected screen construction, while Hearing Test retains its documented execution and result-data gates even though the setup screen itself does not read entitlement? Confirm that moving entry points to Hearing or Meter did not bypass, duplicate, or weaken `ProRouteAccessGate`, `SleepSetupAvailability`, `SleepSetupEntryPolicy`, Camera permission policy, or feature-specific ViewModel, service, and domain gates. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q024 — Adaptive shell insets

Are `shouldUseNavigationRail`, `shouldApplyContentNavigationBarPadding`, `DbCheckNavigationFrame`, `innerPadding`, and `.statusBarsPadding()` composed so content is not double-inset, hidden behind navigation, or given an unnecessary bottom gap on phones, tablets, landscape, and fullscreen routes? A finding needs a concrete window configuration and layout consequence. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q025 — Navigation accessibility

Do all five bar and rail items expose correct selected state, label, role, icon semantics, and touch target size, with meaningful behavior under TalkBack and large font? Verify the shared enum-driven construction and any custom rail item implementation rather than assuming Material defaults cover custom modifiers. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q026 — Navigation test strength

Do `NavigationRoutePolicyTest.kt` and `SettingsGraphContractTest.kt` validate executable behavior, or do any source-string assertions pass while the navigation graph is behaviorally broken? Identify which critical cases require a Compose navigation test or instrumentation test, and label missing runtime coverage as a verification gap unless a concrete bug is otherwise proven. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## C. Trends and Hearing ownership

### Q027 — Trends remains measurement-oriented

Does `AnalyticsScreen.kt` render only Overview, Spectral, and Environment measurement content plus the compact `HearingStatusRow`, with no full Hearing Test, Recovery, Tinnitus, Ambient, Sleep, or Voice Baseline tool cards remaining? Check conditional card-group construction and not merely imports. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q028 — Analytics ViewModel responsibility reduction

Has `AnalyticsViewModel.kt` actually removed hearing-tool repositories and state flows while retaining only measurement analytics, live spectral/environment state, and shared hearing-health summary calculation? Verify constructor dependencies and combine chains for hidden recovery, tinnitus, or sleep ownership. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q029 — Hearing status row navigation

Does `HearingStatusRow` always navigate to `Screen.Hearing.route` from Trends for both data and no-data presentations, without a nested child intercepting clicks or the decorative chevron becoming a separate TalkBack focus target? Inspect its semantics, click modifier, and caller wiring. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q030 — Hearing status presentation parity

Do `HearingHealthCard` on Hearing and `HearingStatusRow` on Trends derive text, status colors, icons, and nullable comparison handling from `HearingHealthPresentation.kt` so the same summary cannot produce contradictory messages? Prove any divergence with a specific `HearingHealthSummary` input. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q031 — Hearing screen section order

Does `HearingScreenContent` present hearing-health status and the latest test, Hearing Test, Recovery, Tinnitus Pitch, Voice Baseline, and then tools in the documented order, with optional Sleep Monitor before Ambient Sounds? Is every visible section reachable and separated using current spacing tokens? Report only an order or reachability defect tied to the live implementation, accepted contract, or accessibility behavior, not personal layout preference. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q032 — Latest Hearing Test data exposure

Does `HearingViewModel.toUiState` intentionally hide prior Hearing Test details from Free users by returning `NoResult`, and does the UI copy avoid falsely claiming that no test exists when the data is merely Pro-gated? Evaluate the product contract, strings, locked presentation, and existing entitlement conventions before calling this a defect. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q033 — Recovery state mapping

Does `mapHearingRecoveryState` correctly distinguish Free locked preview, Pro missing baseline, Pro ready-with-baseline, and Pro result states, including stale or incompatible recovery results? Trace each CTA through the latest full hearing-test baseline, the 1/4/8 kHz recovery mode, `HearingRecoveryService`, and aggregate-only `hearing_recovery_results` persistence. Verify no raw audio or new clinical audiometry data is stored and that UI copy remains personal tracking rather than diagnosis or safety advice. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q034 — Tinnitus profile privacy, gating, and scope

Does Free-state mapping replace `preferences.tinnitusPitchProfile` with an empty `TinnitusPitchProfile()` before it reaches the locked preview, preventing saved Pro data from leaking under an overlay while preserving it in DataStore? Trace preview and save execution so only a user-started `ToneGenerator` path with the current pitch and amplitude policy is possible. The feature must not introduce background playback, a service, sound therapy, symptom-reduction or diagnosis claims, Health Connect writes, raw-audio persistence, or automatic triggers. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q035 — Sleep visibility and readiness semantics

Does the persisted `sleep_card` preference act only as a Pro-effective visibility control for `SleepSetupCta` on both Meter and Hearing, so Free users and Pro users with the toggle off see no Sleep card, while `SleepSetupViewModel` readiness and `startSleepRecording()` depend on the current effective Pro state rather than on visibility? Verify that both entry points share the same policy, a stale or direct route cannot execute while locked, and the current contract does not accidentally reintroduce a Free locked preview that `PROJECT.md` says is hidden. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q036 — Ambient tool visibility, gating, and scope

Is `AmbientSoundCard` deliberately present in Hearing according to the current UI contract, with content and navigation properly Pro-gated and no saved Pro settings exposed under a locked preview? Trace the action through `AmbientSoundPlaybackViewModel`, notification permission, the separate `mediaPlayback` foreground service, audio-focus loss, timer stop, and visible Stop control. The path must not use `RECORD_AUDIO`, `MeasurementForegroundService`, Room history, raw-audio capture, Health Connect, therapy or safety claims, or automatic playback triggers. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q037 — Component ownership after moves

Do all consumers import `AmbientSoundCard`, `HearingRecoveryCard`, `TinnitusPitchCard`, `HearingTestCta`, and hearing-health presentation components from `ui/hearing/components`, with no package cycles or duplicate old files under `ui/analytics/components`? Confirm with repository-wide searches and build evidence. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q038 — Top app bar actions removed consistently

Have the duplicate Settings/Profile actions been removed from Meter, Trends, and History while preserving any unrelated essential actions, and does `DbCheckTopAppBar()` render no empty clickable slot when no action is supplied? Compare screen call sites and shared component semantics. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## D. Hearing-health calculation and presentation semantics

### Q039 — Weekly aggregation unit

Does `HearingHealthSummaryCalculator.calculate` correctly compute a sample-count-weighted energy average from `DailyExposureAverage.avgDb`, or does `sampleCount` represent a unit that makes cross-day weighting scientifically or statistically invalid? Inspect the producer of `DailyExposureAverage` and `DecibelMath.energyAverageDb`; do not substitute intuition for the repository's data contract. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q040 — Filtered invalid days

Does filtering only `sampleCount > 0` adequately exclude invalid `avgDb` values such as NaN, infinity, sentinel zero, or corrupt database results before energy conversion? A finding requires proof that such values can enter `DailyExposureAverage` through current production code or persisted data. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q041 — Sample-count overflow

Can `usableDailyAverages.sumOf { it.sampleCount }` or weighted energy multiplication overflow its numeric type for realistic seven-day measurement volumes, and would overflow cause a null, NaN, or incorrect status? Establish actual types and maximum production rates before reporting. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q042 — Status threshold preservation

Do `NoiseLevel.NORMAL.maxDb`, `NoiseLevel.ELEVATED.maxDb`, and `HearingHealthSummaryCalculator` preserve the intended SAFE, WARNING, and DANGER boundaries, including equality at each threshold and the documented 85 dB persistence boundary? Compare current domain constants, presentation mapping, and focused tests; use historical implementation only as secondary evidence when the relevant git base is actually available. Report a regression only with an exact input and expected status contract. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q043 — No-data state

Does `calculate` return `null` for empty or entirely unusable data so neither Trends nor Hearing displays a false SAFE status or misleading `0 dB` weekly average? Verify every caller's fallback fields, especially `AnalyticsViewModel`'s separate `weeklyAverageDb = ... ?: 0f` and `hasWeeklyExposureData`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q044 — Today matching by day start

Is equality between `dailyAverage.dayStartMs` and `dayStartMs(nowMs, zoneId)` valid for the repository query's timezone and day-bucketing contract, including DST transitions, timezone changes, and stored timestamps generated under a previous device timezone? Prove whether mismatch is possible in current data production. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q045 — Multiple rows for today

Can `getDailyAveragesLast7Days()` produce more than one `DailyExposureAverage` for the same `dayStartMs`, and if so does `firstOrNull` make `todayVsWeekPercent` dependent on query ordering rather than aggregating all current-day data? Inspect the repository/DAO query and grouping guarantees before reporting. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q046 — Today-versus-week formula meaning

Is `((todayAverageDb - weeklyAverageDb) / weeklyAverageDb * 100).toInt()` the intended percent comparison for logarithmic decibel values, or should the UI communicate a dB delta or energy ratio instead? This is a defect only if an existing product/domain contract defines a different metric; otherwise record the scientific interpretation as a product question rather than inventing a code fix. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q047 — Percent truncation behavior

Is Kotlin `Float.toInt()` truncation toward zero intentional for positive and negative percentages, and do presentation strings remain accurate around values such as `3.85`, `-3.85`, and less than one percent? Compare `HearingHealthSummaryCalculatorTest` and `HearingHealthPresentationTest` before alleging an off-by-one error. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q048 — Missing current-day comparison

When weekly history exists but no row exists for the current local day, does nullable `todayVsWeekPercent` propagate through both full card and compact row without fabricated zero, stale comparison text, formatting exceptions, or blank layout gaps? Inspect all constructors, previews, and string formatting. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q049 — Zero and negative weekly average branch

Can a valid dataset produce `weeklyAverageDb <= 0f`, and if so is returning comparison `0` semantically correct, or should the comparison be absent? Establish production-domain validity and downstream meaning before treating the defensive branch as faulty. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q050 — Clock and timezone testability

Does `HearingViewModel.buildUiState` calling `System.currentTimeMillis()` and `ZoneId.systemDefault()` on every combined emission create nondeterminism, timezone inconsistency, or a stale day boundary because no flow emits at midnight? Determine whether the screen can remain open across midnight and show yesterday as “today,” and distinguish testability concerns from confirmed user-visible defects. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q051 — Shared calculator caller parity

Do Analytics and Hearing pass the same seven-day data, current time basis, and timezone to `HearingHealthSummaryCalculator`, ensuring identical output for the same moment? Trace both callers and any different collection timing that could cause visible disagreement between tabs. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q052 — Calculator regression coverage

Does `HearingHealthSummaryCalculatorTest.kt` cover empty input, zero-sample days, unequal sample counts, threshold boundaries, today absent, timezone/DST behavior, negative comparison, and rounding/truncation, with expectations derived from independent math rather than copying implementation? Missing cases are verification gaps unless they expose a proven defect. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## E. Hearing ViewModel, entitlement, and Voice Baseline data flow

### Q053 — Flow lifecycle and sharing

Does `HearingViewModel` collect the current preference, exposure, latest-test, recovery, recording, sound-detection, and other repository flows with one well-scoped lifecycle, avoiding duplicate collectors, leaks, unnecessary upstream work, stale combined values, and unhandled cancellation for the lifetime of the top-level Hearing destination? Compare the actual flow types, Hilt ViewModel scope, and focused tests instead of relying on an old count of combined flows. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q054 — Initial state correctness

Can the default `HearingUiState()` briefly expose misleading Free/no-data UI while entitlement and repository flows are still loading, because the model has no explicit loading state? Verify actual first-emission timing and app startup gating before reporting a visible flicker or unauthorized UI state. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q055 — Combined-flow error handling

If any flow collected by `HearingViewModel` throws, can the Hearing state collector permanently stop without an error state or retry, and can the current repository or service implementations actually emit such an exception? Trace `catch`, cancellation, upstream Room or DataStore behavior, collector restart, and UI fallback across every current input flow. Report only a reachable failure, not a generic observation that `combine` can terminate. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q056 — Pro downgrade redaction

When `isProUser` changes from true to false while Hearing is visible, are saved test, recovery, tinnitus, Voice Baseline, sound-detection, and Sleep states immediately redacted or locked in one atomic UI-state emission, with no live Pro data retained in remembered component state? Inspect `buildUiState` and child composables. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q057 — Pro upgrade restoration

When entitlement changes from Free to Pro, do the same repository flows re-emit or does the preferences emission alone rebuild state from the latest combined cached values so saved hearing data appears without reopening the screen? Confirm `combine` semantics and repository flow types. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q058 — Voice Baseline UI gate

Does `HearingUiState.canCalibrateVoiceBaseline` exactly enforce `isProUser && isRecording && soundDetectionEnabled`, and is that state used to disable or redirect every calibration action exposed by `VoiceBaselineCard`? Check whether a locked or disabled UI element can still invoke `onCalibrate` through semantics or nested clicks. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q059 — Voice Baseline execution gate race

Does `calibrateVoiceBaseline()` safely re-check current entitlement, `audioSessionManager.isRecording.value`, and effective sound-detection state at click time, and can any of those values change between the check and `captureVoiceBaseline(isProUser = true)` in a way that bypasses service-level policy or stores an invalid capture? Prove a reachable race and its consequence before reporting. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q060 — Voice Baseline capture argument

Is hardcoding `captureVoiceBaseline(isProUser = true)` safe because the caller has just checked effective entitlement, or does the service rely on the passed value in a way that can be stale or forged relative to the centralized entitlement policy? Inspect `AudioSessionManager.captureVoiceBaseline` and all callers. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q061 — Voice Baseline capture failure feedback

When `captureVoiceBaseline` returns `null` because there are too few speech samples, Sound Detection becomes unavailable, recording stops, entitlement changes, or another precondition fails, does the current Hearing UI provide the feedback required by its live contract, or does the action appear to succeed silently? Establish the current card state, enabled semantics, and user-facing error conventions first; consult the former Settings implementation only as secondary history, not as the source of truth. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q062 — Voice Baseline persistence failure

If `preferencesRepository.updateVoiceBaseline` throws or is cancelled, is the failure surfaced, retried, or safely ignored according to existing repository conventions, and could the card falsely show success from a transient local capture? Trace flow re-emission and prior implementation behavior. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q063 — Duplicate calibration submissions

Can rapid repeated taps launch concurrent `updateVoiceBaseline` writes with different captures, and if so is last-writer behavior intentional and safe? Check button enabled state, click throttling conventions, DataStore serialization, and whether a realistic harmful outcome exists. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q064 — Baseline metadata consistency

Does `buildUiState` prevent inconsistent combinations such as null `voiceBaselineLevelDb` with non-zero sample count or non-null captured time, and does `VoiceBaselineCard` render all persisted combinations defensively without claiming a baseline exists when its primary value is absent? Use actual preference defaults and migration behavior. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q065 — Locale-aware Voice Baseline formatting

Does `VoiceBaselineCard.voiceBaselineLabel` use `currentLocale` and a localized resource for decimals, units, and sample counts, avoiding `Locale.US`, raw concatenation, or grammatically invalid output in Finnish and English? Validate representative values and large-font wrapping. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q066 — Hearing ViewModel test realism

Do `HearingViewModelTest.kt` fakes model hot/cold flow behavior, entitlement changes, recording transitions, sound-detection changes, repository emissions, capture nullability, and persistence accurately enough to catch broken production wiring? Treat an unrealistic fake as a finding only when it can mask a concrete production defect; otherwise report a coverage gap. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## F. Settings graph, shared ViewModel, and page ownership

### Q067 — One Settings ViewModel per graph entry

Does every Settings child route obtain the same `SettingsViewModel` through `navController.getBackStackEntry(Screen.Settings.route)` and `hiltViewModel(parentEntry)`, including after process restoration, direct navigation to a child, and legacy redirect? Determine whether a direct child route can exist without its parent graph entry and cause `getBackStackEntry` to fail. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q068 — Settings Home unnecessary ViewModel creation

`settingsRoutes` calls `settingsGraphViewModel(navController, backStackEntry)` on Home and discards the returned value; is this intentional eager initialization required for shared events/state, or unnecessary work with observable side effects? Inspect `SettingsViewModel.init`, billing collectors, backup state, and lifecycle expectations before reporting. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q069 — `remember(backStackEntry)` scoping

Is remembering the parent entry using only the child `backStackEntry` key sufficient when the `navController` or Settings graph entry changes, or can a stale parent entry survive graph recreation and scope the ViewModel incorrectly? Prove this under actual Compose/navigation lifecycle behavior rather than hypothetical recomposition. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q070 — Settings hub row completeness

Does `SettingsHomePage` expose exactly five rows—Calibration, Notifications & alerts, Data & privacy, Display, and Pro & About—with correct route constants, Material icons, localized default-English labels, full-row click targets, and no obsolete emoji section headings? Verify any Finnish strings that are intentionally inside the limited launch baseline, but do not require full Finnish parity that `PROJECT.md` explicitly does not claim. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q071 — Settings page ownership matrix

Does each Settings concern appear on exactly one intended page? Calibration should own audio preferences, response and frequency weighting, input devices, and calibration profiles; Notifications & alerts should own exposure and peak alerts, audible alarm, spoken TTS risk prompt, passive monitoring, threshold, and schedule; Data & privacy should own Health Connect, CSV, WAV disclosure and default, local backup and restore, approximate session location, Clear history, and lockscreen privacy; Display should own theme, waveform, refresh rate, and feature toggles; Pro & About should own purchase, debug Force Free, version, and About. Report duplicated or missing ownership only after tracing conditional content, Free/Pro presentation, and action wiring. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q072 — Octave calibration child ownership

Does `settings/calibration/octave` correctly behave as a child of Calibration with shared state, proper Back behavior, locked preview for Free users, and no standalone top-level hub row that bypasses its context? Inspect `octaveCalibrationPresentation` and navigation wiring. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q073 — Shared state persistence across pages

When a user edits a setting on one page, navigates to another Settings page, and returns, does the graph-scoped ViewModel retain transient dialogs/messages and observe persisted state correctly without cross-page messages appearing in the wrong context? Exercise purchase, export, passive-monitoring, calibration, backup, and restore feedback. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q074 — Settings graph teardown

When leaving Settings for another top-level destination and later restoring the Settings stack, is the shared ViewModel lifetime consistent with saved back-stack state, and are collectors or pending one-shot events duplicated or replayed unexpectedly? Verify actual navigation ownership and event-flow semantics. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q075 — Back buttons and system Back parity

Do all Settings child pages use `SettingsPageScaffold` and `DbCheckTopAppBar` so the visible Back button and Android system Back produce the same destination, including Octave → Calibration and direct Pro & About navigation? Test both normal hub entry and upgrade entry stacks. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q076 — Top app bar semantics

Does the optional navigation icon in `DbCheckTopAppBar` have a localized content description, at least a 48 dp target, correct enabled semantics, and no invisible placeholder when `onBack` is null? Check every new Settings page and non-Settings caller affected by the shared component change. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q077 — Page scroll and nested scroll behavior

Can every Settings page reach all content at 360 × 800 dp and font scale 1.5 without nested-scroll conflicts, bottom-nav obstruction, clipped dialogs, or inaccessible final controls? Ground the answer in the actual scaffolds, modifiers, screenshot baselines, and if possible a semantics or UI test. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q078 — Settings source decomposition quality

Does the current decomposition into Settings pages, action models, launchers, and private helpers genuinely improve ownership, or has it retained coupled page logic, repeated actions, cross-page message assumptions, or unclear lifecycle behavior that creates a concrete correctness or maintenance defect? Do not report current file length or the mere presence of a shared helper as a bug. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## G. Settings callbacks, permissions, privacy, and destructive actions

### Q079 — Microphone permission sequence for passive monitoring

In `SettingsNotificationsPage`, when microphone permission is initially missing and then granted, does the callback continue exactly once into notification-permission handling without prematurely starting passive monitoring or launching duplicate permission requests after recomposition? Trace `handleStartPassiveMonitoring` and launcher callbacks. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q080 — Microphone denial behavior

When microphone permission is denied temporarily or permanently, do `passiveMonitoringPermissionDenied`, `onPassiveMonitoringPermissionDenied`, the retry action, and app-settings action produce accurate UI without starting the service? Check lifecycle resume behavior after returning from system settings. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q081 — Notification permission callback semantics

In the current passive-monitoring flow, does the Android 13+ notification-permission launcher start the foreground sample only through the intended callback path, and is its behavior correct for both granted and denied Boolean results? If denial still proceeds, verify that this is an explicit policy consistent with current Android foreground-service behavior, manifest requirements, and user disclosure; if denial blocks, verify the pending start is cleared and retry works. Report a defect only from the current callback and platform contract, not from the historical implementation. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q082 — Pre-Android-13 notification path

Does `hasPostNotificationsPermission` plus `requestPostNotificationsPermissionIfNeeded` behave correctly on API levels where `POST_NOTIFICATIONS` does not exist as a runtime permission, ensuring passive monitoring starts immediately without a launcher that never returns? Inspect API guards and tests. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q083 — Passive-monitoring confirmation controller

Does `PassiveMonitoringStartConfirmationController` require the intended privacy confirmation exactly when enabling passive monitoring, survive recomposition/configuration changes correctly, prevent double confirmation, and clear pending state after accept or dismiss? Compare the controller test with its real composable integration. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q084 — Passive-monitoring execution exclusion

Do the Settings UI and execution path prevent passive monitoring from conflicting with an active manual or Sleep measurement or another microphone owner? For backup and restore, first establish from the current code whether the `MeasurementDatabaseGate` contract intentionally includes aggregate-only passive monitoring; then determine whether any permitted overlap can race its Room write or snapshot. Trace denial, service-start failure, stop, empty-sample, persistence failure, and cleanup branches so no partial aggregate or leaked audio resource remains. Do not infer the policy solely from disabled UI or from the fact that passive mode bypasses `AudioSessionManager.startSession()`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q085 — Notification schedule and evaluator semantics

Do `NoiseNotificationsSection`, `NoiseNotificationSchedule`, and the alert runtime preserve independently selected weekdays, locale-correct labels, start and end minute-of-day values, disabled states, same-start-and-end meaning the full selected day, and overnight windows whose after-midnight portion belongs to the previous active day? Confirm the schedule is evaluated before exposure, actual-dose, projected-dose, or peak alerts; a successfully delivered alert type is deduplicated for the rest of that session; and a failed delivery can retry only after the documented cooldown. UI mixed selection, persistence, evaluator input, and notification delivery must not diverge. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q086 — Warning and disclosure placement

Does `CompactDisclosureInfo` show the required microphone and user-started passive foreground-sample, raw WAV, approximate location, Health Connect, lockscreen-public, export, backup, restore, and destructive-action disclosures at the point where the user makes the relevant decision, with accurate scope, tone, and readable semantics? Identify a missing or misleading disclosure only by tracing the actual sensitive action and current privacy contract; do not describe passive monitoring as automatic background sampling when the implementation is explicitly user-started. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q087 — Disclosure implementation accessibility

Does `CompactDisclosureInfo` provide sufficient color contrast, non-color cues, appropriate semantics, readable line wrapping, and non-clickable behavior unless an action is explicitly supplied? Validate dark/light themes and font scale 1.5. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q088 — Location permission state

In `SettingsDataPrivacyPage`, do `coarseLocationPermissionGranted`, `coarseLocationPermissionDenied`, the permission launcher, and `ON_RESUME` refresh correctly distinguish first request, temporary denial, permanent denial, and grant-from-settings without showing a stale warning or requesting excessive precision? Trace what the downstream feature actually needs. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q089 — Health Connect refresh and actions

Does refreshing Health Connect status on every `ON_RESUME` and wiring `healthSyncActions` preserve provider availability, permission state, Free noise-write enablement, Pro heart-rate overlay, write-toggle behavior, install/update and manage intents, and user-facing error handling without duplicate launchers or stale results? Confirm every action reaches the same graph-scoped `SettingsViewModel` and the `HealthConnectService` port, and distinguish this UI lifecycle review from the payload contract reviewed separately later. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q090 — CSV intent collection lifecycle

Does `LaunchedEffect(csvShareChooserTitle) { viewModel.csvExportIntents.collect { ... } }` collect exactly once per page instance, avoid replaying an old intent after navigation or configuration change, and correctly call either `onCsvShareStarted` or `onCsvShareUnavailable`? Inspect the event flow type and producer semantics. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q091 — Backup and restore UI safety wiring

Were backup creation, restore selection, restore confirmation, active-measurement feedback, execution through the shared database gate, safety and validation messages, restart callback, and error paths preserved when Data & privacy was split into its own page? Trace `onRestartAfterRestore` from `MainActivity` through `DbCheckNavHost`, the page and action model, and `SettingsViewModel.confirmRestoreBackup`. Treat the UI active-recording check as fast feedback, not as the sole concurrency protection. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q092 — Clear-history destructive flow

Does the moved Clear history UI remain available to both Free and Pro users, require confirmation, block during an active measurement, delete inactive sessions and their cascaded child rows, remove associated app-private WAV files and passive-monitoring summaries, leave `filesDir/backups` untouched, and surface success or error only on the owning page without replay? Confirm execution gates and tests. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q093 — WAV preference and disclosure

Does the WAV default remain effective OFF for Free users and default OFF generally, avoid exposing or changing persisted Pro state through a locked preview, and show the raw-audio and storage disclosure before opt-in? Confirm the UI can enable recording only through the effective `isProUser && wavRecordingDefaultEnabled` policy, never implies MediaStore storage or background recording, and routes Session Detail share or delete actions only when a real app-private WAV exists. The writer and file lifecycle are reviewed separately later. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q094 — Lockscreen privacy controls and notification visibility

Do `LockscreenMeterSectionActions`, `SettingsViewModel`, and `NotificationPrivacyPolicy` keep the general Pro lockscreen-meter toggle distinct from the separate default-OFF “show publicly” opt-in? Verify the measurement notification remains `VISIBILITY_PRIVATE` unless effective Pro, the rich lockscreen meter, and public display are all enabled; Free or disabled states use the ordinary private notification; stale persisted state cannot expose readings after downgrade; and the custom collapsed and expanded `RemoteViews` show only the intended current dB, peak, duration, and noise-level indicator. Inspect execution checks, notification building, and UI semantics. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q095 — Pro purchase action lifecycle

Does `rememberProPurchaseAction(viewModel)` use the current Activity and context, survive recomposition safely, avoid retaining a destroyed Activity, fetch valid ProductDetails before launch, and route `Completed`, `Pending`, `Cancelled`, `AlreadyOwned`, and `Failed` events to `SettingsPurchaseFeedback` on the owning visible page? Verify `PENDING` never unlocks Pro, `AlreadyOwned` refreshes the purchase snapshot, feedback is consumed once, and navigation between Settings pages cannot lose or replay the event. Prove any lifecycle leak or lost event from code and flow behavior. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q096 — Timed message cross-page clearing

Can `TimedMessageEffect` or `SettingsDataPrivacyMessageEffects` clear a newer message because an older delay completes after state changes, or clear feedback belonging to another Settings page sharing the same ViewModel? Analyze effect keys, cancellation semantics, and message fields with a concrete sequence. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## H. Meter layout, interaction, and retained functionality

### Q097 — Meter content order

Does `MeterReadoutContent` preserve the documented core order—mode selector, session status, gauge, selected-mode summary or Live details, stats, and Sound Reference—followed by fixed controls, while rendering the optional Pro-effective `SleepSetupCta` from the current shared visibility policy without making any Meter feature unreachable? Validate dB Meter and Dosimeter, recording and idle, Sleep enabled and disabled, and large-font states. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q098 — Fixed controls under constrained height

Does the outer weighted scroll column plus non-scrolling `MeterControlsSection` keep reset/start/share/camera controls visible and operable at 360 × 800 dp, smaller supported heights, landscape, IME/system inset changes, and font scale 1.5 without forcing the readout to an impossible zero height? Provide exact constraints for any failure. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q099 — No-default-scrolling acceptance criterion

At the specified 360 × 800 dp core idle state, do the compact gauge, spacing-token branches, collapsed Sound Reference row, top app bar, bottom navigation, and fixed controls fit without an initial scroll offset, while expanded content and the optional enabled Sleep CTA can scroll without hiding the fixed controls? Validate the actual screenshot viewport, runtime density, insets, and feature flags rather than summing nominal dp values incompletely. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q100 — Large-font compact gauge

Is the `fontScale > 1f` threshold for choosing a 176 dp gauge appropriate and stable, or can minor user font scaling unexpectedly shrink the gauge while larger accessibility scales still clip other text? A defect requires a demonstrated layout or accessibility failure, not disagreement with the threshold. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q101 — Height-based compact policy

Does `maxHeight < 720.dp` measure the intended content viewport after scaffold insets, and can device cutouts, gesture navigation, landscape, or split-screen produce a wrong layout branch? Test boundary values near 720 dp and report only a concrete consequence. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q102 — Expandable state restoration

Do `rememberSaveable` states for Live details and Sound Reference restore safely across rotation/process recreation without making both expanded sections render controls off-screen or restoring expansion for a semantically different session/mode? Confirm expected product behavior and saved-state keys. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q103 — LiveActivityCard retained chart and waveform

Does collapsed/expanded `LiveActivityCard` preserve the existing live chart and waveform data, recording/idle states, content descriptions, and update performance, with no state collection removed when the UI moved behind an expander? Trace the ViewModel state and child components. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q104 — Live details header interaction

Is the entire `LiveDetailsHeader` a single accessible toggle with correct expanded/collapsed state semantics, at least 48 dp target, decorative chevron semantics, and no conflicting nested click targets? Validate TalkBack announcement and keyboard/switch access. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q105 — Sound Reference collapsed behavior

Does `SoundReferenceCollapsedRow` show a correct nearest reference and range for empty markers, out-of-range dB values, ties, and rapidly changing values, while remaining a single 48 dp accessible expander? Trace calculations back to `MeterUiState` rather than assuming display code owns them. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q106 — Sound Reference expanded geometry

Do `SoundReferenceRail`, marker positions, nearest highlighting, and range labels handle the complete supported dB domain without NaN, overlap, clipping, reversed order, or hardcoded geometry that conflicts with `ChartTokens` or theme spacing? Establish actual inputs and screenshot evidence. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q107 — Meter and Hearing Sleep visibility ownership

Do Meter and Hearing both consume the same Pro-effective `sleep_card` visibility source and route to the same `sleep/setup` destination without duplicating Sleep business logic, while `SleepSetupViewModel` remains the owner of readiness and setup options? Check `MeterUiState`, `HearingUiState`, their collectors, both CTA callbacks, Settings toggle behavior, and tests for stale one-screen-only assumptions. Report any missing entry point, duplicate policy, or route divergence. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q108 — Meter permission and service-start contract

Does the current Meter path ask at startup only for a missing microphone permission, request Android 13+ notification permission as part of a user-initiated recording start, and avoid launching microphone capture from an impermissible background state? Trace denial, retry, return-from-settings, service-start failure, and successful start through `MeterScreen`, permission policy, `MeasurementForegroundService`, and `AudioSessionManager`, including the requirement that foreground promotion succeeds before AudioRecord starts. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q109 — Meter control enablement

Do `MeterControlsState.isShareEnabled`, `.isCameraOverlayEnabled`, recording state, Pro entitlement, and action wiring prevent invalid share, Camera Overlay, reset, or start/stop operations without hiding legitimate Free behavior? Trace each callback through `MeterScreenActions` to `MeterViewModel`, foreground-service intents, or navigation, and verify Camera Overlay still has route, permission, and execution gates rather than relying on the button state alone. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q110 — Meter contract-test brittleness

Do `MeterScreenLayoutContractTest.kt` and `MeterExpandableComponentsContractTest.kt` verify layout and interaction meaningfully, or are they source-string checks that can pass despite a visually broken or inaccessible screen? Identify concrete missing runtime assertions as verification gaps unless an actual defect is demonstrated. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## I. History layout, data semantics, and session-card accessibility

### Q111 — Empty History compactness

Does `HistoryUiState.Empty` render only the compact `EmptyState` with Meter CTA and no fake Last 24 Hours chart allocation, oversized skeleton, or unreachable content at 360 × 800 dp? Verify screenshot and composable branching. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q112 — History error-state safety and semantics

Does `HistoryUiState.Error` and its `HistoryError` or `EmptyState` rendering use only a localized, resourced fallback plus the intended Meter action, while keeping metadata-save errors separate inside otherwise successful History content? Trace every repository or mapping failure through `UserFacingError` or the current error mapper and prove that no raw exception message, file path, SQL text, provider detail, or empty inaccessible description can reach the user. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q113 — Last 24 Hours empty-versus-data split

Does `Last24HoursChart` choose `Last24HoursEmptyCard` only when the data contract says no meaningful samples exist, and avoid drawing invented axes/averages for empty or all-invalid input? Verify `last24HoursChartHeaderState` and caller-provided aggregates together. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q114 — Last 24 Hours time window and DST

Do X-axis labels, subtitle, chart positions, and accessibility description consistently use `windowStartMs` and `windowEndMs`, including DST days that are not exactly 24 clock hours and locale-specific time formatting? A finding requires a reproducible timestamp set. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q115 — Chart accessibility summary

Does `last24HoursChartContentDescription` communicate data availability, average, maximum, and trend without forcing TalkBack through decorative Canvas elements, and is the no-data card described accurately? Check duplicate semantics from visible text and parent merging. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q116 — SessionCard trailing statistics

Does the compact trailing `SessionCardStats` preserve peak and average meaning, correct labels/units, locale formatting, and readable contrast without squeezing the session name, tags, Sleep badge, or duration at 360 dp and font scale 1.5? Use the large-font screenshot baseline and semantics. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q117 — SessionCard click target interactions

Are the card-open action, emoji/edit action, Pro-lock action, and any nested controls unambiguous, at least 48 dp, and free from click propagation that opens Session Detail while the user intended to edit metadata? Test Free and Pro cards with and without custom names/tags. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q118 — Remembered editing session freshness

Can `editingSession` remembered in `HistorySuccessContent` become stale when the sessions list updates, causing a metadata save to overwrite newer name, emoji, or tags, or to edit a deleted session? Trace sheet arguments and repository update semantics; report only a reachable harmful sequence. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q119 — LazyColumn identity and state

Do `recentSessionItems` and other History list items use stable unique keys so inserts, deletions, metadata updates, and “view all” changes preserve the correct remembered/animated state and scroll position? Inspect the actual `LazyListScope` implementation and demonstrate any identity collision. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q120 — Automatic session naming and historical time

Do `hourOfDay`, `autoSessionName`, and `autoEmoji` use the session's persisted historical start offset when it is known, or an explicit safe fallback when it is not, rather than silently reinterpreting old sessions in the device's current timezone? Verify consistency among History, Session Detail, PNG/PDF display text, and persisted metadata while keeping machine-readable CSV timestamps in UTC. Separate a pre-existing naming policy from a v13 offset-mapping defect. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## J. Shared components, design tokens, localization, and accessibility

### Q121 — Tertiary button casing

Does `DbCheckButton.kt` remove forced uppercase only from the tertiary style while preserving intended casing for primary/secondary styles and localized strings, and do any callers now depend on manual uppercase text that produces inconsistent UI? Compare component screenshots and all style callers. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q122 — Design-token compliance

Do Meter, Trends, Hearing, History, Settings, and shared components use the current centralized tokens when an equivalent exists: `DbCheckSpacing` semantic spacing, `DbCheckRadii`, typography, card and chip variants, `Motion.kt` durations, and `ChartTokens.kt` geometry and alpha? Report a hardcoded value only when a matching project token exists and the divergence has a concrete design-system, accessibility, screenshot, or behavior impact. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q123 — Shared card and chip grammar

Do new surfaces extend `DbCheckCard`, `DbCheckChip`, `InlineStatusRow`, `DbCheckAlertDialog`, or other established shared components instead of rebuilding equivalent shape/color/padding behavior, and are any deviations intentional for navigation rows or charts? Do not force abstraction when semantics genuinely differ. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q124 — Pro locked-preview contract

Do features that are meant to preview while locked use the established `ProLockOverlay` grammar, keep rich preview content non-interactive beneath one scrim and upgrade action, and avoid loading or exposing live Pro data? Check Hearing Test, Recovery, Tinnitus Pitch, Ambient Sounds, Voice Baseline, octave calibration, Pro audio preferences, export, WAV, lockscreen, and display features. Do not require a locked preview for Sleep, whose current contract hides the card unless Pro and enabled, or for Free Health Connect noise sync, which is intentionally available. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q125 — Minimum target sizes

Do every new or changed row, icon button, chip, expander, toggle, card CTA, Back action, session edit action, and navigation item meet the 48 dp minimum interactive size under actual modifiers and parent constraints? Name the exact composable and measured/derived size for any violation. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q126 — TalkBack focus and roles

Are decorative icons and chevrons excluded from accessibility focus, clickable containers assigned suitable Button/Switch/Tab roles, selected and expanded states announced, and nested text not duplicated excessively? Review `HearingStatusRow`, Settings hub rows, Live details, Sound Reference, navigation items, and SessionCard specifically. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q127 — Color and non-color communication

Do SAFE/WARNING/DANGER, selected/unselected chips, locked states, success/error/info disclosures, chart series, and session statistics remain distinguishable without color alone and meet contrast in light/dark themes? Use token values or visual/accessibility tooling rather than subjective screenshots alone. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q128 — English resources and Finnish launch baseline

Does the current default-English resource set remain complete while the intentionally limited `values-fi` launch baseline satisfies only its documented scope, placeholder and plural compatibility, and inline-text guard? Recount the current 812 strings and 11 plurals in `values`, and 112 strings and 2 plurals in `values-fi`, instead of assuming full key parity. Verify new user-facing text is resourced, format-argument types are valid, and any omitted Finnish translation is either outside the declared launch baseline or explicitly documented. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q129 — Locale-sensitive display and machine formats

Are calibration offsets, octave frequencies, Voice Baseline values, notification schedule times, hearing percentages, dB values, History labels, and report text formatted with the active locale and correct unit or plural resources, while machine-readable CSV timestamps remain `DateTimeFormatter.ISO_INSTANT` UTC and numeric fields retain locale-independent decimal points? Check `String.format` locale choices, concatenation, historical session offsets, and locale capture during recomposition. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q130 — Font scale and text clipping

At font scales 1.0, 1.5, and a larger accessibility scale such as 2.0, do headings, bottom navigation labels, Settings rows, chips, stat columns, cards, buttons, and dialogs wrap or scroll without clipping, overlap, ellipsis that hides essential meaning, or inaccessible controls? Distinguish screenshot-covered 1.5 behavior from unverified larger scales. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q131 — Right-to-left resilience

Even though current resources are English and Finnish, do changed layouts use start/end alignment, directional icons, and logical padding so an RTL locale would not reverse navigation meaning or clip content? Report only concrete misuse of left/right APIs or non-mirroring icons, not absence of RTL screenshots alone. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q132 — Dynamic state announcements

When recording state, dB values, permission errors, purchase feedback, export status, passive-monitoring status, or Voice Baseline availability changes, are important changes announced appropriately without causing continuous TalkBack spam from high-frequency values? Inspect live-region semantics and update frequency. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## K. Compose state, performance, concurrency, and lifecycle

### Q133 — Recomposition stability

Do the changed UI state classes, action containers, component parameters, and collections remain stable/skippable according to the regenerated debug and release Compose stability reports, or did the restructure introduce unstable parameters that cause high-frequency Meter or Analytics recomposition? Tie any finding to a specific report entry and caller update rate. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q134 — Action object allocation

Are `HearingScreenActions`, `MeterScreenActions`, `HistorySuccessActions`, `NoiseNotificationsSectionActions`, and other action containers recreated in a way that materially defeats Compose skipping or captures stale values? Measure or infer from stable function references and report only meaningful recomposition or correctness impact. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q135 — High-frequency flow isolation

Does adding Hearing and restructuring Meter/Analytics accidentally make broad screens recompose at audio sample frequency when only a gauge, chart, or live chip needs updates? Trace StateFlow aggregation and composable parameter boundaries; a finding needs an update-rate path and affected subtree. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q136 — Main-thread work

Do new presentation calculations, daily energy aggregation, profile formatting, chart geometry, screenshot-only data creation, or repository operations execute expensive work on the main thread during recomposition or flow collection? Quantify realistic workload before reporting a performance issue. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q137 — One-shot event collection

Do purchase, CSV share, backup/restore, clear-history, calibration, and passive-monitoring one-shot events have lifecycle-safe collection and consumption after Settings became a multi-page graph with one shared ViewModel? Look for lost events when the owning page is absent and replayed events when it returns. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q138 — Remembered local state ownership

Are local UI states such as expanders, permission-denied flags, editing session, dialogs, and selected page controls stored at the right level and keyed to the data they represent, or can state bleed between users, sessions, pages, or changed list items? Demonstrate an actual state mismatch. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q139 — Cancellation and navigation

If the user leaves a screen while calibration, export intent creation, purchase launch, permission request, or repository loading is pending, do callbacks target a valid lifecycle owner and avoid navigating, showing dialogs, or starting services twice on return? Trace coroutine and launcher ownership. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q140 — Process death restoration

After process death on a Settings child, Hearing screen, expanded Meter card, History edit sheet, or legacy upgrade redirect, can Navigation Compose, `rememberSaveable`, Hilt ViewModels, and SavedStateHandle restore to a valid state without missing graph parents or repeating destructive actions? Report unsupported cases as verification gaps unless a failure is reproduced or proven. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## L. Tests, screenshot baselines, and verification quality

### Q141 — Unit-test execution result

Can the current checkout reproduce a clean `:app:testDebugUnitTest` result, and do generated JUnit XML files confirm the executed test count and zero failures rather than relying on source inventories, console summaries, dated local reports, or the single `UiDocumentationContractTest` run recorded in `PROJECT.md`? If the suite is not rerun, mark it unverified rather than inventing a defect. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q142 — Navigation behavioral coverage

Do tests cover bar and rail selection, five-item order, Settings child selected state, all fullscreen hidden-navigation routes, Session Detail History selection, reselection to root, stack restoration, legacy redirects, direct Pro route, and Hearing-flow returns? For every missing case, state whether it is merely missing coverage or exposes a confirmed bug. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q143 — Hearing ViewModel coverage

Do tests cover Free/Pro transitions, empty/data state, unequal-weight energy averages, latest-test redaction, all Recovery states, tinnitus redaction, Sleep visibility, Voice Baseline gating, null capture, successful persistence, and recording/sound-detection transitions? Do not equate an uncovered branch with an implementation defect. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q144 — Settings permission coverage

Do tests exercise microphone missing/granted/denied, notification missing/granted/denied, pre-Android-13 behavior, app-settings return, confirmation accept/dismiss, and active-recording conflicts for passive monitoring? Identify whether source-contract tests can actually catch callback-order regressions. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q145 — Settings page ownership tests

Can `SettingsScreenStructureTest`, `SettingsGraphContractTest`, and `SettingsReviewBehaviorTest` prove that every feature appears exactly once and its action reaches the correct ViewModel method, or do regex/source-string assertions create false confidence? Propose stronger tests only for material risks, and do not call weak tests a production bug by themselves. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q146 — Screenshot inventory integrity

Do `ComponentScreenshotTests.kt` and `FullScreenScreenshotTests.kt` currently contain 59 component and 39 full-screen `@PreviewTest` functions, with exactly 98 matching reference images and no orphaned, missing, or silently duplicated baseline names? Recount functions and files recursively from the current checkout instead of trusting documentation, and distinguish 34 ordinary full-screen states from the 5 `fontScale = 1.5f` states. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q147 — Required 360 × 800 matrix

Do full-screen references include light and dark 360 × 800 coverage for Meter idle/recording/Dosimeter, Trends Overview/Spectral/Environment, Hearing Free/Pro, History empty/sessions, Settings hub, and every Settings child including octave calibration? List any missing exact state as coverage, not automatically as a code defect. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q148 — Large-font screenshot matrix

Which five `fontScale = 1.5f` full-screen previews exist in the current source, do they use the intended 360 × 800 viewport, and do they cover the highest-risk combinations of Meter controls, Hearing cards, History rows, Settings permission or privacy content, and long localized text? Identify the actual preview names rather than assuming the former five-screen set. Treat an uncovered screen as a verification gap unless source or runtime evidence demonstrates clipping. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q149 — Baseline review quality

Were changed screenshot baselines visually reviewed for content loss, clipping, selection ambiguity, theme contrast, unexpected scroll position, and missing controls rather than accepted solely because `updateDebugScreenshotTest` regenerated them? Name the exact image and visible defect for any finding. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q150 — Preview realism

Do preview states use realistic combinations of entitlement, feature flags, recording state, permissions, data availability, and long localized text, or can impossible state combinations make the screenshots pass while production fails? Tie any unrealistic fixture to the specific bug it could mask. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q151 — Screenshot contract-test robustness

Does `FullScreenScreenshotContractTest.kt` validate viewport, theme, state coverage, and preview/reference parity without depending on brittle source text that can be gamed by comments or dead preview functions? Separate a fragile test implementation from an actual missing screenshot artifact. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q152 — Documentation count and source-hierarchy synchronization

Does `UiDocumentationContractTest.kt` derive and enforce screenshot counts and current architecture facts without elevating exact prose in `PROJECT.md`, `AGENTS.md`, `STATUS.md`, `memory/MEMORY.md`, or historical specs above live code? Can comments or stale duplicated wording satisfy the test while semantic contradictions remain, especially around Room v13, MediaPipe, five destinations, Settings graph ownership, Finnish baseline scope, and 98 screenshot references? Report only a concrete current mismatch or harmful brittleness. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q153 — Static analysis and local-report verification

Can the current checkout reproduce clean direct `:app:ktlintCheck`, `:app:detekt`, and Android lint results for the relevant source sets? Follow `AGENTS.md`: do not run the user-owned `lc` or `sc` wrappers unless explicitly authorized, and treat gitignored reports dated 2026-07-26 as snapshots rather than proof of a later `HEAD`. When reports are supplied, verify the actual files and parser summaries agree and do not assume a wrapper produced a file it no longer creates. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q154 — Build and dependency-verification contract

Can `:app:assembleDebug` complete from the current checkout with JDK 21, compile and target SDK 37, Kotlin 2.4.10, AGP 9.3.1, generated resources, Gradle dependency verification, the root buildscript lock, project lockfiles, and pinned transitive constraints? Confirm `settings.gradle.kts` fails safely when verification metadata or the buildscript lock is missing or empty. If the build is not executed, state it as unverified rather than inferring breakage from version changes. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q155 — Device, instrumentation, and release-sign-off gaps

Which critical behaviors still require emulator, physical-device, Play, or signed-artifact verification after the recorded API 36 and API 37 smoke tests: full TalkBack, all navigation back stacks, purchase and pending flows, signed release AAB installation, Play upload, Qodana compatibility, backup/restore process restart, adaptive rail, process death, and privacy-sensitive share paths? Confirm there is no `androidTest` source set before describing instrumentation coverage. Treat the recorded microphone, CameraX, FileProvider, Health Connect, WAV-share, and 16 KB checks as dated evidence for those artifacts, not automatic proof of the current `HEAD`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q156 — Regression-test mutation resistance

For the most important current contracts, would a plausible one-line mutation—wrong Hearing route, removed Pro or execution gate, reversed permission callback, duplicated Settings ViewModel, nullable comparison converted to zero, hidden Sleep CTA on one owner, current-timezone substitution for a null historical offset, skipped database-gate release, reintroduced legacy Task Audio dependency, or missing FileProvider read grant—cause a focused test to fail for the right reason? Use targeted mutation reasoning; do not demand exhaustive mutation testing without a demonstrated risk. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## M. Integration, privacy, and release-readiness review

### Q157 — End-to-end Free user journey

Starting from a clean Free-user launch, can the user navigate through all five top-level destinations, use the allowed Meter, seven-day History, Health Connect noise-sync, notification, and passive-monitoring paths, understand visible locked previews without seeing saved Pro data, observe that Sleep CTA is hidden rather than falsely previewed, reach Pro & About, cancel or fail a purchase, and return without broken stacks, stale feedback, or accidental entitlement changes? Record the exact first failing step if one exists. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q158 — End-to-end Pro Hearing journey

Can a Pro user start a measurement, enable Sound Detection, open Hearing, capture Voice Baseline, launch Hearing Test, Recovery, Tinnitus Pitch, Ambient Sounds, and Sleep, complete or back out of each, and return to a correctly selected Hearing hub with fresh state? Also verify the same enabled Sleep CTA from Meter reaches the identical setup policy. Distinguish unavailable headphones, audio, notification, or device verification from a confirmed wiring failure. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q159 — End-to-end Settings data-safety journey

Can a user move among all Settings pages while changing calibration, input, notification schedule, audible alarm, spoken prompt, passive monitoring, Health Connect, location, WAV, display, and privacy preferences; exporting CSV; creating or restoring backup; clearing history; and purchasing Pro without actions being replayed, lost, executed under stale entitlement, or shown on the wrong page? Require explicit authorization before destructive tests against valuable data and keep device-only gaps separate from confirmed wiring defects. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q160 — Sensitive-data non-disclosure

Does the current checkout avoid logging, screenshotting, previewing, or exposing saved hearing and tinnitus profiles, Voice Baseline values, approximate location, Health Connect data, raw WAV contents or paths, backup paths, session metadata, billing tokens, signing material, or debug Sentry credentials to Free users or unauthorized external components? Trace UI state redaction, logs, previews, notifications, FileProvider roots, exported components, and user-facing error mapping. Do not limit the search to the historical restructure diff. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q161 — Entitlement single source of truth

Do all current UI, route, ViewModel, service, repository, and domain gates consume effective entitlement from `ProEntitlementPolicy`, `UserPreferences.isProUser`, or an established gate without introducing a second policy, debug/release divergence, or UI-only authorization? Trace Free-to-Pro and Pro-to-Free transitions, nullable billing startup state, debug Force Free, direct routes, saved Pro data redaction, and every protected execution path named in `PROJECT.md`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q162 — External contract consistency

Are the current external contracts internally consistent rather than merely unchanged from the historical UI diff: package and version metadata; compile and target SDK 37; manifest permissions, `<queries>`, exported flags, optional camera features, and foreground-service types; FileProvider authority and roots; Room schema version 13 and backup identity hashes; CSV/PDF/PNG/WAV formats; Health Connect record types; notification visibility; and release-signing inputs? Report only a code-, configuration-, schema-, artifact-, or behavior-backed mismatch. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q163 — Release documentation accuracy

Do current documentation and QA contracts accurately describe the live checkout: five top-level destinations and the internal `analytics` route, Meter and Hearing Sleep entry points, graph-scoped Settings ownership, Room v13 historical offsets, MediaPipe Tasks Audio 0.10.35 and 16 KB compatibility, target SDK 37, current dependency and workflow constraints, the limited Finnish launch baseline, 306 production Kotlin files, the source inventory of 224 test Kotlin files, 207 test-class declarations and 1,092 `@Test` annotations, and 98 screenshot baselines split 59 plus 39? Recount quantities and cite exact contradictory lines; do not treat the document's own counts as proof. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## N. Startup, measurement runtime, and persistence coordination

### Q164 — Startup theme first-frame contract

Does `MainActivity` wait for the first `UserPreferences` emission, synchronize the package-specific night mode from `ThemeMode` on Android 12+, use the intended resource-theme startup behavior on Android 11 and older, and only then render `DbCheckTheme` and `DbCheckNavHost`? Trace cold start, process recreation, configuration change, missing or delayed preference emission, and theme updates so the app neither flashes the wrong theme nor remains blank. Compare `MainActivityThemeTest` with the current activity and resource-theme implementation. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q165 — Billing startup snapshot preservation

Does `BillingEntitlementSource.isPurchased` begin nullable and cause `ProFeatureManager` to write DataStore only after a verified `true` or `false`, preserving persisted Pro entitlement through startup, connection errors, and delayed purchase queries? Verify startup and `onResume` refresh, reconnect and `ITEM_ALREADY_OWNED` paths, acknowledgement of `PURCHASED`, rejection of `PENDING`, debug Force Free through `ProEntitlementPolicy`, and immediate UI re-evaluation without a restart. Compare `ProFeatureManagerStartupTest`, `BillingManagerTest`, `ProEntitlementPolicyTest`, and `SettingsViewModelPurchaseTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q166 — Foreground promotion before audio capture

Does `MeasurementForegroundService` call `ServiceCompat.startForeground(...)` with the microphone foreground-service type before any `AudioSessionManager.startSession()` or `AudioRecord` work, and abort cleanly if promotion fails? Verify Android 14+ while-in-use microphone restrictions, user-initiated start, manifest permissions, notification construction, `START_NOT_STICKY`, repeated start and stop intents, and cleanup when foreground promotion, permission, or audio start fails. No path may begin microphone capture first and attempt foreground promotion afterward. Compare `MeasurementForegroundServicePolicyTest`, `MeterStartupPermissionPolicyTest`, and `AudioSessionManagerAudioStartTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q167 — Measurement and database-maintenance exclusion

Does `MeasurementDatabaseGate` atomically prevent a manual or Sleep measurement lifecycle from overlapping local backup or restore, and prevent a held backup or restore permit from reaching `AudioRecord` start? Trace permit acquisition and release through successful start, denied start, partial initialization, service failure, reset, normal completion, interrupted-session recovery, cancellation, backup retry, restore failure, and process restart. UI recording checks are not sufficient evidence; verify the shared execution-time gate and owner-specific release behavior. Compare `LocalBackupManagerTest` and the database-gate cases in `AudioSessionManagerAudioStartTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q168 — Session transaction atomicity and active-slot uniqueness

Do `SessionRepository.recordActiveSessionMeasurements(...)` and `completeSessionWithMeasurements(...)` write pending measurement rows together with the active-session runtime summary or final completion in one Room transaction, while the schema and DAO contract preserve one active session slot? Trace the lifecycle and flush mutexes, concurrent persistence ticks, stop waiting for an in-flight flush, final unsaved reading, transaction failure, and retry so session summaries cannot get ahead of or behind their measurement rows. Compare `SessionRepositoryTransactionContractTest` and `RoomSchemaContractTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q169 — Interrupted recovery versus normal completion

Does startup recovery close an interrupted active session from persisted data without emitting `completedSessionIds`, auto-navigating to Session Detail, inventing a historical end-zone offset, or replaying normal completion side effects? Conversely, does a successful user-initiated stop publish exactly one completion ID after Room completion and applicable side effects, while reset and failure paths remain silent? Trace `DbCheckApplication`, `AudioSessionManager`, repository completion methods, Health Connect, and navigation collectors. Use the focused AudioSessionManager, repository, startup, and navigation tests that actually exercise recovery and completion; name any missing branch as a verification gap. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q170 — Audio value separation and energy math

Do `AudioEngine.DecibelReading`, `SessionStats`, live dosimeter state, persisted measurements, and reports keep raw RMS (`instantDb`), selected-weighting RMS (`weightedDb`), A-weighted exposure values, and C-weighted `peakDb`/LCpeak semantically separate? Verify energy averaging, 0–130 dB clamping, frequency-weighting filter output, NIOSH/OSHA inputs, peak-event detection, and report labels. Report a defect only with a concrete path where one value is substituted for another or a linear average is used where the contract requires energy averaging. Compare `SessionReportCalculatorTest`, `DosimeterCalculatorTest`, `CsvExportFormatterTest`, and the current weighting and audio-math tests. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q171 — Meter refresh-rate isolation

Does `MeterRefreshRate` change only the Compose UI emission cadence at the documented HIGH, STANDARD, and LOW intervals, leaving the 44.1 kHz AudioRecord stream, 4,096-sample chunks, weighting-filter continuity, spectral and Sound Detection inputs, live dosimeter accumulation, alert evaluation, and Room persistence policy unchanged? Verify `MeasurementPersistenceSampler` still persists at its fixed cadence while forcing the first reading, the 85 dB boundary crossing, new weighted or LCpeak maxima, and the final unsaved reading. Trace preference changes during an active session and confirm disabling the Dosimeter card returns only the UI mode to dB Meter without restarting or reconfiguring capture. Compare the current Meter ViewModel, persistence-sampler, AudioEngine runtime-preference, and dosimeter tests that actually observe these cadences. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q172 — Audio input discovery, routing, and metadata

Do audio-input discovery and routing normalize built-in, USB, Bluetooth, and wired devices consistently; apply the selected device only for effective Pro users; call `AudioRecord.setPreferredDevice(...)` before `startRecording()`; and fall back to the built-in microphone when a saved external device disappears without overwriting the saved preference? Verify selected and actually routed device metadata is published only after successful audio start and persisted to the session accurately, while Free execution receives an effective null selection. Compare current audio-input discovery, routing, runtime-preference, session-mapper, and metadata tests; do not infer routing success from a UI selection alone. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## O. Sound detection, raw audio, and specialized runtime paths

### Q173 — YAMNet resampling and window continuity

Does `YamnetAudioWindowAdapter` convert the continuous 44.1 kHz PCM16 stream to mono 16 kHz float windows using the documented 96-tap windowed-sinc anti-alias filter, a cutoff no higher than Nyquist or 7,500 Hz, 15,600-sample windows, and a 7,680-sample hop? Verify chunk-boundary continuity, source-history and absolute-position handling, startup and reset behavior, deterministic window counts, amplitude scaling, and that no PCM or float window is persisted. Compare `YamnetAudioWindowAdapterTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q174 — MediaPipe classifier lifecycle and mapping

Does `MediaPipeSoundClassifier` lazily create the MediaPipe Tasks Audio runtime on the first non-empty classification request, feed one-channel 16 kHz `AudioData`, map categories through `SoundClassificationPolicy` and its confidence threshold, serialize `classify` and `close` on the same lock, and release native resources idempotently? Trace model or class-map load failure, empty windows, repeated initialization, concurrent classify/close, cancellation, and reuse after failure so native state cannot leak, race, or return stale labels. Compare `MediaPipeSoundClassifierTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q175 — 16 KB native-library compatibility

Does the packaged debug and release artifact use MediaPipe Tasks Audio rather than the removed `tensorflow-lite-task-audio:0.4.4` runtime, and do every packaged native library's ZIP alignment and ELF `PT_LOAD` alignment satisfy Android's 16 KB page-size requirements for the shipped ABIs? Inspect resolved dependencies and the actual APK or AAB contents, not only catalog names or a previous physical-device result. Confirm `NativeLibraryCompatibilityTest` cannot pass while a legacy 4 KB library remains transitively packaged. Compare `NativeLibraryCompatibilityTest`, Gradle dependency resolution, and the actual packaged artifacts. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q176 — Sound-detection execution and persistence gates

Does `SoundDetectionWindowFanout` run inference only under effective `isProUser && soundDetectionEnabled`, with `AudioSessionManager` rather than `AudioEngine` owning classifier execution, and does persistence additionally require the separate opt-in? Verify only aggregated `sessionId`, timestamp, label, and confidence events are written, the same label is not repeated until a label change or empty result resets it, Free downgrade stops and redacts the feature, session end cleans up state, and raw PCM, float windows, spectra, or model tensors never reach Room or exports. Compare `SoundDetectionRepositoryTest`, `MediaPipeSoundClassifierTest`, `YamnetAudioWindowAdapterTest`, and `AudioEngineRuntimePreferenceTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q177 — WAV writer and file lifecycle

Does the WAV path start only for effective Pro plus explicit opt-in, stream PCM16 to `filesDir/wav_recordings`, finalize RIFF and data lengths on normal stop, and delete partial files on start failure, write failure, reset, cancellation, or abnormal cleanup? Verify Session Detail detects only complete session-owned files, shares them as `audio/wav` through the allowed FileProvider root with `ClipData` and temporary read permission, deletes only the selected session file, Clear history removes files for deleted sessions, and no WAV is copied to MediaStore or export cache. Compare `PcmWavWriterTest`, `WavRecordingFileStoreTest`, `PrivacyConfigTest`, and the relevant Session Detail action tests. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q178 — Audible-alarm policy and playback lifecycle

Do `AudibleAlarmPolicy` and `AudibleAlarmEvaluator` preserve the documented default threshold, duration, and cooldown behavior, reset the duration window below threshold, and require a new full duration after cooldown before another trigger? Verify execution requires effective Pro plus opt-in, the interactive-screen and proximity guard is checked at playback time, `MediaPlayer` uses alarm audio attributes and transient focus, and player plus focus are released on completion, error, focus loss, start failure, session stop, and service cleanup. Preview playback must not mutate evaluator state. Compare `AudibleAlarmPlaybackControllerTest` and `MediaPlayerAudibleAlarmPlayerContractTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q179 — Spoken risk-prompt boundaries

Does `TtsRiskPromptEvaluator` trigger only from dosimeter `DOSE` or `PROJECTED_DOSE` risk events and only when effective Pro, the TTS opt-in, Sound Detection availability, and a latest hearing-test baseline are all present? Verify peak alerts, ordinary threshold alerts, Free state, missing baseline, and disabled Sound Detection cannot speak; `AndroidTextToSpeechPlayer` uses `QUEUE_FLUSH`, handles initialization and shutdown safely, and the manifest query is present. Spoken copy must remain a cautious exposure prompt and must not persist audio, model windows, hearing-test changes, or new Room data. Compare `TtsRiskPromptPolicyTest`, `TtsRiskPromptControllerTest`, `AndroidTextToSpeechPlayerContractTest`, and `VoiceVolumeWarningPolicyTest` where shared guards are involved. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q180 — Passive monitoring aggregate-only runtime

Does user-started passive monitoring use its foreground-service intent and `PassiveMonitoringManager` without calling `AudioSessionManager.startSession()`, create no session or measurement rows, and persist only the documented aggregate sample on stop? Verify permission denial, notification stop, service failure, repeated start, empty sample, Clear history, and cleanup. The path must not start WAV, spectral, Sound Detection, audible-alarm, voice-warning, exposure-alert, Health Connect, or completed-session navigation logic, and no boot receiver, timer, WorkManager job, or other automatic background microphone trigger may exist without a new explicit product and privacy decision. Compare `PassiveMonitoringManagerTest`, `PassiveMonitoringAggregatorTest`, and `PassiveMonitoringRepositoryTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q181 — Sleep recording, metadata, and result nullability

Does Sleep setup derive `Loading`, `Locked`, or `Ready` from effective Pro entitlement rather than the `sleep_card` visibility toggle, preserve only the documented 6 h, 8 h, and 10 h targets plus keep-awake option, and re-check readiness before starting? Verify Sleep uses the normal measurement foreground-service path while writing one-to-one `sleep_sessions` metadata, applies target-duration auto-stop, keeps the screen on only when opted in, marks History and Session Detail by the same session ID, and feeds results, insights, PDF, and CSV without converting unavailable measurements into zero-valued claims. Compare `SleepSetupEntryPolicyTest`, `SleepSetupViewModelTest`, `SleepResultsCalculatorTest`, and `SleepInsightsCalculatorTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q182 — Camera Overlay permission and capture scope

Does Camera Overlay remain an optional-camera, Pro-gated route with explicit granted, denied, permanently denied, unavailable, and binding-failure states? Verify its live dB overlay only observes `AudioEngine.decibelFlow` during an already active measurement and never starts or stops that measurement. Photo sharing may write a temporary raw JPEG, but must produce the shared burned-in PNG under `cache/exports` with FileProvider grants and cleanup. Silent video must not call `withAudioEnabled()` or claim the Compose overlay is burned into the MP4 when no render or post-processing path exists. Compare `CameraPermissionPolicyTest`, `CameraXPreviewBindingContractTest`, `CameraOverlayShareGeneratorTest`, and `CameraOverlayShellContractTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## P. Historical time, reports, exports, backup, Health Connect, and release chain

### Q183 — Room v13 historical-offset contract

Does `MIGRATION_12_13` add nullable `startUtcOffsetSeconds` and `endUtcOffsetSeconds` without backfilling legacy rows from the current device zone, and does the exported schema plus backup validator include the correct v13 identity? Verify new-session start and normal completion resolve offsets for their actual timestamps, interrupted recovery leaves an unknowable end offset null, and entity-to-domain-to-report mappings preserve nullability. When start and end offsets differ, intermediate display times must not reuse a provably wrong offset; formatters should fall back explicitly to UTC. Compare `SessionTimeZoneSchemaContractTest`, `SessionTimeZoneOffsetsTest`, `SessionMappersTest`, `ReportTextFormatterTest`, and `HealthConnectNoiseDosePayloadTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q184 — Shared report calculation and weighting semantics

Do Session Detail, PDF, PNG, Health Connect notes, and Sleep report adapters consume the same `SessionReportData` produced by `SessionReportCalculator`, rather than recalculating metrics independently? Verify equivalent-level labels match A, B, C, Z, and ITU-R 468 weighting; NIOSH TWA, dose, projected dose, and 85 dBA events appear only for A-weighted data; LCpeak remains C-weighted; missing octave, heart-rate, location, or Sleep data renders as unavailable rather than zero; and export-time calibration context is not misrepresented as historical session calibration. Compare `SessionReportCalculatorTest`, `DosimeterCalculatorTest`, `ExportPdfReportUseCaseTest`, and `ShareResultsGeneratorTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q185 — CSV paging, selection, and machine format

Does `ExportCsvUseCase` use the same contract for all-session and selected-session exports, read measurements and sound-detection events in bounded pages with deterministic ordering, and produce the session summary, measurement, and optional sound-event files without loading all raw rows into memory? Verify metadata columns, `peak_db`, optional Sleep data, and aggregated sound labels are mapped correctly; all machine timestamps use locale-independent `ISO_INSTANT` UTC; numeric fields use decimal points; and an absent optional sound CSV is explicit rather than silently mistaken for export failure. Compare `ExportCsvUseCaseTest`, `CsvExportFormatterTest`, and the current DAO query-contract tests. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q186 — FileProvider roots, cleanup, and read grants

Do `ExportFileCache`, manifest provider metadata, and `file_paths.xml` share one authority-suffix and path contract that exposes only `cache/exports/` plus app-private `files/wav_recordings/`, with the provider non-exported and `grantUriPermissions=true`? Verify generated filenames cannot escape those roots and files older than 24 hours are cleaned on the next export or share. Every actual share intent for cached PNG, CSV, Camera output, WAV, or any cached PDF must use a `content://` URI with the appropriate stream or streams, `ClipData`, and temporary read permission; the PDF `CreateDocument` path must instead write only to the user-selected content URI. No file URI, broad root, or unintended app-private path may be exposed. Compare `PrivacyConfigTest`, `ShareResultsGeneratorTest`, `ExportPdfReportUseCaseTest`, `WavRecordingFileStoreTest`, and Camera share tests. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q187 — Backup snapshot and restore durability

Does `LocalBackupManager.copyCheckpointedDatabase(...)` checkpoint WAL with `TRUNCATE`, exclude writers with a SQLite transaction, copy only after the WAL sidecar is empty, retry the snapshot no more than three times, and fail closed if the database keeps changing? Verify temp-file writing calls `fd.sync()`, final replacement is atomic when supported, backup validation occurs before the final move, restore validates the chosen file and a pre-restore safety backup, stale WAL/SHM sidecars are removed, operations are serialized by mutex and `MeasurementDatabaseGate`, and a successful restore invokes the process-restart callback only after replacement. Clear history must not delete backups. Compare `LocalBackupManagerTest`, `PrivacyConfigTest`, and the database-gate cases in `AudioSessionManagerAudioStartTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q188 — Health Connect payload and data boundaries

Does normal completed-session sync write an actively recorded `ExerciseSessionRecord` with `EXERCISE_TYPE_OTHER_WORKOUT`, a stable session-specific client record ID, report-derived notes, and only known persisted start and end offsets when Health Connect is enabled? Verify interrupted, reset, and failure completions do not write; hearing-test sync remains an intentional `Skipped` no-op; heart-rate access is read-only, permission-gated, and Pro-only in Session Detail or PDF; provider status and payload errors are mapped to safe user-facing messages; and no unsupported native noise or audiometry record is invented. Compare `HealthConnectManagerTest`, `HealthConnectNoiseDosePayloadTest`, and `HealthConnectHeartRateMapperTest`. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

### Q189 — Dependency, CI, and release supply-chain contract

When dependencies, plugins, scanners, Android versions, or packaged runtime assets change, do `libs.versions.toml`, Gradle lockfiles, `buildscript-gradle.lockfile`, `gradle/verification-metadata.xml`, transitive security constraints, scanner lockfiles, YAMNet model and class-map paths, and Manrope/Space Grotesk license notices remain synchronized for all relevant platforms? Verify GitHub Security's OWASP job is not misrepresented as CVE analysis, local `sc` remains the Dependency-Check evidence, the CodeQL nightly bundle URL and SHA-256 are updated as a verified pair, actions remain commit-pinned with `persist-credentials: false`, Sonar's Dependabot secret behavior is intentional, Qodana stays explicitly non-blocking until a decision changes it, non-PR release builds require every signing secret, and debug Sentry credentials or code cannot enter release artifacts. Compare `GradleWrapperSecurityTest`, `FontLicenseNoticeTest`, `QodanaCiCompatibilityTest`, `ReleaseSigningQaTest`, `BillingProductionQaTest`, workflow files, lockfiles, packaged assets, and the latest explicitly supplied local scanner reports. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.

## Q. Final synthesis

### Q190 — Final objective verdict

After answering Q001–Q189, is there any confirmed defect that blocks release, any confirmed non-blocking defect, or only verification gaps, dated evidence, and intentional product decisions? Produce separate sections for confirmed findings, `No confirmed issue found` results, verification gaps, and optional product or policy questions. Deduplicate overlapping findings, identify the earliest failing invariant and downstream consequences, and do not convert uncertainty, missing device access, stale documentation, preference, or AI-generated suspicion into mandatory code work. **Standalone review brief:** Review only this invariant in the current dBcheck checkout. Primary evidence is live production code and build configuration; then current tests, Room schemas, generated QA artifacts, and `PROJECT.md`. Historical plans do not override live code. Inspect the owner, all direct callers and consumers, DI bindings, and any relevant manifest, resource, schema, workflow, and focused test contracts. Trace every success, failure, cancellation, retry, process-recovery, and entitlement branch that can affect the invariant. Do not modify code, dependencies, baselines, suppressions, or documentation. Confirm a defect only with an exact `file:line` or symbol, an executable path, concrete impact, and a focused reproduction or regression test. Otherwise answer `No confirmed issue found` or name a verification gap. Missing tests, stale plans, and bot suggestions are not defects by themselves. AI reviewers can hallucinate, so do not invent a fix or force a change.
