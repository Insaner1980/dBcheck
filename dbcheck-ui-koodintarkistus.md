# dBcheck UI Restructure — Evidence-Driven Code Review Questions

## How to use this document

Every numbered question below is self-contained. Copy one complete `Qxxx` block into one review thread; no separate Review target and purpose text or general evidence rules need to be copied with it. The common final-output template at the end is only for someone synthesizing the answers from multiple threads.

## A. Review scope, change inventory, and source-of-truth discipline

### Q001 — Reviewable change boundary

Does `git diff b5252aee5d45f17171a7ef2a6788e476f187e3b6..15d1c6a2486518f1a891404b6ad25d0cbf7b929c` contain only changes required by the UI restructure, its tests, screenshot baselines, Compose stability baselines, and mandatory architecture documentation, or are there unrelated behavioral changes hidden among the 139 changed files? Classify every suspicious file by purpose and prove any scope violation from the actual diff rather than filename alone. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q002 — Intended architecture boundary

Does the diff honor `dbcheck-ui-rakenteen-muutos.md`'s explicit boundary that Room schemas, DataStore keys, repository/service interfaces, export formats, and entitlement policy must remain unchanged? Verify the relevant directories and generated schemas, and distinguish the new pure `domain/hearing/HearingHealthSummaryCalculator.kt` from prohibited persistence or service changes. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q003 — No accidental feature deletion

For every feature moved out of Analytics or the old monolithic Settings screen—Hearing Test, Hearing Recovery, Tinnitus Pitch, Ambient Sound, Sleep Monitor, Voice Baseline, calibration, notifications, Health Connect, export, backup/restore, lockscreen privacy, display preferences, Pro purchase, and About—can a reviewer trace an intact route, UI entry point, callback, ViewModel method, and downstream service/repository path in the new structure? Report only a feature that is demonstrably unreachable or disconnected. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q004 — Duplicate implementation check

Did moving cards and page content create duplicate business logic, duplicate preference writes, duplicate route constants, or duplicate hearing-health calculations instead of one source of truth? Inspect moved component packages, `AnalyticsViewModel`, `HearingViewModel`, `SettingsPages.kt`, and their former locations; do not label intentionally separate presentation mappings as duplication without proving equivalent repeated behavior. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q005 — Dead code and stale callers

After deleting `SettingsScreen.kt`, moving hearing components under `ui/hearing/components`, and removing recovery/tinnitus/sleep ownership from Analytics, are there stale imports, unused state fields, orphaned tests, unreachable composables, obsolete resource strings, or old route callers still compiled or referenced? Use repository-wide symbol searches and compiler evidence; absence of a textual reference is not by itself proof that a resource is safely removable. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q006 — Documentation hierarchy

Where `dbcheck-ui-rakenteen-muutos.md`, `dbcheck-ui-restructure-plan.md`, `UI-SPEC.md`, `PROJECT.md`, `AGENTS.md`, and `memory/MEMORY.md` differ, do the live documents consistently describe the merged code and clearly preserve intentional compatibility details such as the internal `analytics` route? Identify only contradictions that could mislead future implementation or review work. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q007 — Generated artifact legitimacy

Are the large changes in `app/stability/app-debug.stability`, `app/stability/app-release.stability`, and screenshot reference PNGs explainable by the public Compose API and intentional visual changes, or do they baseline an unintended stability or rendering regression? Compare source changes and validation tasks before claiming that a generated diff is defective. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q008 — Reviewability of task reports

Do `.superpowers/sdd/task-8-report.md` and `task-9-report.md` accurately record the work and verification without being treated as production sources of truth or introducing repository noise contrary to project conventions? A finding requires a concrete maintenance, disclosure, or correctness impact, not a preference about report files. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

## B. Top-level navigation, adaptive shell, and back-stack behavior

### Q009 — Five-destination order

Does `BottomNavDestination.entries` in `BottomNavDestination.kt` define exactly Meter, Analytics-as-Trends, Hearing, History, and Settings in that visible order, and do both `BottomNavBar` and `DbCheckNavigationRail` consume the enum order without maintaining a second divergent list? Verify labels and icons in both compact and expanded-width shells. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q010 — Analytics route compatibility versus visible Trends label

Does `Screen.Analytics.route == "analytics"` remain stable for saved navigation state and internal compatibility while every user-visible navigation label, heading, content description, screenshot, and live document says “Trends” where intended? Do not report internal class/package names as user-facing defects unless they leak into UI or external contracts. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q011 — Hearing route registration

Is `Screen.Hearing` registered exactly once as a top-level `composable`, connected to `HearingScreenActions`, selectable from bar and rail, and restorable after switching tabs? Trace each action from the nav graph to its intended non-top-level destination and upgrade route. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q012 — Settings graph destination identity

Does the `navigation(startDestination = Screen.Settings.HOME_ROUTE, route = Screen.Settings.route)` graph work correctly with `BottomNavDestination.SETTINGS.screen.route == "settings"`, given that `settings` is a graph route rather than a leaf composable? Verify first selection, reselection, restoration, deep links or direct navigations, and selected-state calculation. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q013 — Selected top-level route mapping

Does `selectedTopLevelRouteFor` correctly map `history/detail/{sessionId}` and concrete detail routes to History, all current `settings/...` pages and the legacy query route to Settings, and only the actual Hearing root to Hearing? Look for false positives from `startsWith`, such as an unrelated route sharing a prefix, and prove whether such a route can exist. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q014 — Fullscreen navigation visibility

Do Hearing Test setup/active/results, Recovery setup/active, Tinnitus Pitch, Ambient playback, Sleep setup, and Camera Overlay all return `null` from `selectedTopLevelRouteFor`, thereby hiding bar and rail, while Session Detail intentionally stays within the History shell? Validate actual route strings and rendered shell behavior, not only policy unit tests. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q015 — Reselect-to-root semantics

Does `topLevelNavigationPolicy` reset a nested Settings page to `settings/home` when Settings is reselected, leave an already-rooted destination unchanged through `isAlreadyAtRoot`, and avoid duplicate graph entries? Exercise Settings home, each Settings child, History detail, and every ordinary root separately. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q016 — History detail reselection

When the current route is Session Detail and the user reselects History, does `topLevelNavigationPolicy` navigate to the History root with the intended state behavior, rather than treating the detail route as already at root or restoring the detail immediately? Confirm against a real back-stack sequence and `NavigationRoutePolicyTest`. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q017 — Cross-stack state restoration

Does the combination of `popUpTo(findStartDestination().id) { saveState = policy.shouldRestoreState }`, `launchSingleTop`, and `restoreState = policy.shouldRestoreState` preserve scroll and nested state when switching between different top-level stacks without reviving stale nested destinations on reselection? Check Meter → Settings child → Hearing → Settings and History detail → Meter → History sequences. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q018 — Same-stack navigation state policy

Is `shouldRestoreState = !isSameTopLevelStack` correct for all callers of the shared `navigateTo` lambda, especially navigation to Settings from a Settings child and navigation targets that are not members of `BottomNavDestination`? Prove a defect with an actual caller and failing sequence rather than theorizing about an impossible target. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q019 — Legacy Settings redirect with `showPro=false`

Does navigating to `settings?showPro=false` remove the legacy entry inclusively and leave a clean `settings/home` back stack, including process-restored argument handling and repeated invocations? Verify `settingsLegacyRedirectPlan(false)`, the `LaunchedEffect`, and whether the unused `backTargetRoute` field indicates a real behavioral omission or merely harmless data. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q020 — Legacy Settings redirect with `showPro=true`

Does `settings?showPro=true` reliably create the intended Home → Pro & About stack so Back returns to Settings Home, with no duplicate Home or Pro entries when invoked from Meter, another Settings page, or an existing Settings stack? Compare `navigateToUpgrade` and the legacy composable redirect because they implement similar two-step navigation. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q021 — Direct `createRoute` contract

Do all callers correctly understand that `Screen.Settings.createRoute(false)` returns `settings/home` and `createRoute(true)` returns `settings/pro_about`, while `ROUTE_WITH_ARGS` exists only for legacy compatibility? Search all callers for assumptions that `createRoute` still builds a query string or that Back from direct Pro navigation must always land on Home. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q022 — Hearing-flow return destinations

After moving hearing ownership out of Analytics, do successful completion, cancellation, system Back, and explicit Back actions from Hearing Test, Recovery, Tinnitus, Ambient, and Sleep flows return to the intended Hearing hub rather than Analytics, Meter, or an empty stack? Inspect every `popBackStack`, `navigate`, and completion callback in `DbCheckNavHost.kt` and the nested screens. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q023 — Pro navigation gates after route movement

Do the navigation-level gates for Tinnitus, Ambient, Hearing Recovery, and Sleep still execute before protected screen construction, while ViewModel/service execution gates remain intact as defense in depth? Confirm that moving entry points to Hearing did not bypass, duplicate, or weaken `ProRouteAccessGate` and `SleepSetupAvailability`. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q024 — Adaptive shell insets

Are `shouldUseNavigationRail`, `shouldApplyContentNavigationBarPadding`, `DbCheckNavigationFrame`, `innerPadding`, and `.statusBarsPadding()` composed so content is not double-inset, hidden behind navigation, or given an unnecessary bottom gap on phones, tablets, landscape, and fullscreen routes? A finding needs a concrete window configuration and layout consequence. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q025 — Navigation accessibility

Do all five bar and rail items expose correct selected state, label, role, icon semantics, and touch target size, with meaningful behavior under TalkBack and large font? Verify the shared enum-driven construction and any custom rail item implementation rather than assuming Material defaults cover custom modifiers. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q026 — Navigation test strength

Do `NavigationRoutePolicyTest.kt` and `SettingsGraphContractTest.kt` validate executable behavior, or do any source-string assertions pass while the navigation graph is behaviorally broken? Identify which critical cases require a Compose navigation test or instrumentation test, and label missing runtime coverage as a verification gap unless a concrete bug is otherwise proven. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

## C. Trends and Hearing ownership

### Q027 — Trends remains measurement-oriented

Does `AnalyticsScreen.kt` render only Overview, Spectral, and Environment measurement content plus the compact `HearingStatusRow`, with no full Hearing Test, Recovery, Tinnitus, Ambient, Sleep, or Voice Baseline tool cards remaining? Check conditional card-group construction and not merely imports. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q028 — Analytics ViewModel responsibility reduction

Has `AnalyticsViewModel.kt` actually removed hearing-tool repositories and state flows while retaining only measurement analytics, live spectral/environment state, and shared hearing-health summary calculation? Verify constructor dependencies and combine chains for hidden recovery, tinnitus, or sleep ownership. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q029 — Hearing status row navigation

Does `HearingStatusRow` always navigate to `Screen.Hearing.route` from Trends for both data and no-data presentations, without a nested child intercepting clicks or the decorative chevron becoming a separate TalkBack focus target? Inspect its semantics, click modifier, and caller wiring. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q030 — Hearing status presentation parity

Do `HearingHealthCard` on Hearing and `HearingStatusRow` on Trends derive text, status colors, icons, and nullable comparison handling from `HearingHealthPresentation.kt` so the same summary cannot produce contradictory messages? Prove any divergence with a specific `HearingHealthSummary` input. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q031 — Hearing screen section order

Does `HearingScreenContent` present status, Hearing Test, Recovery, Tinnitus, Voice Baseline, and tools in the planned order, and is every section reachable and visually separated using existing spacing tokens? Report only an order or reachability defect tied to the accepted plan or accessibility behavior, not personal layout preference. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q032 — Latest Hearing Test data exposure

Does `HearingViewModel.toUiState` intentionally hide prior Hearing Test details from Free users by returning `NoResult`, and does the UI copy avoid falsely claiming that no test exists when the data is merely Pro-gated? Evaluate the product contract, strings, locked presentation, and existing entitlement conventions before calling this a defect. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q033 — Recovery state mapping

Does `mapHearingRecoveryState` correctly distinguish Free locked preview, Pro missing baseline, Pro ready-with-baseline, and Pro result states, including stale or incompatible recovery results? Trace the domain models and UI actions to verify that the displayed CTA is valid in each state. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q034 — Tinnitus profile privacy and gating

Does Free-state mapping replace `preferences.tinnitusPitchProfile` with an empty `TinnitusPitchProfile()` before it reaches the locked preview, preventing saved Pro data from leaking under an overlay while still preserving data in storage? Check UI previews and semantics as well as state mapping. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q035 — Sleep card visibility semantics

Is `sleepCardVisible = isProUser && preferences.sleepCardEnabled` consistent with the accepted meaning that `sleep_card` controls only Meter/Hearing CTA visibility, and does it accidentally make the locked Sleep preview impossible for Free users despite `SleepSetupCta(isLocked = !state.isProUser)`? Treat this as a defect only if current product documentation or existing behavior requires a visible Free upgrade preview. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q036 — Ambient tool visibility and gating

Is `AmbientSoundCard` deliberately always present in Hearing while its content and navigation are properly Pro-gated, and does the locked preview avoid receiving live or saved Pro-only information? Trace both the card action and the destination gate. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q037 — Component ownership after moves

Do all consumers import `AmbientSoundCard`, `HearingRecoveryCard`, `TinnitusPitchCard`, `HearingTestCta`, and hearing-health presentation components from `ui/hearing/components`, with no package cycles or duplicate old files under `ui/analytics/components`? Confirm with repository-wide searches and build evidence. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q038 — Top app bar actions removed consistently

Have the duplicate Settings/Profile actions been removed from Meter, Trends, and History while preserving any unrelated essential actions, and does `DbCheckTopAppBar()` render no empty clickable slot when no action is supplied? Compare screen call sites and shared component semantics. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

## D. Hearing-health calculation and presentation semantics

### Q039 — Weekly aggregation unit

Does `HearingHealthSummaryCalculator.calculate` correctly compute a sample-count-weighted energy average from `DailyExposureAverage.avgDb`, or does `sampleCount` represent a unit that makes cross-day weighting scientifically or statistically invalid? Inspect the producer of `DailyExposureAverage` and `DecibelMath.energyAverageDb`; do not substitute intuition for the repository's data contract. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q040 — Filtered invalid days

Does filtering only `sampleCount > 0` adequately exclude invalid `avgDb` values such as NaN, infinity, sentinel zero, or corrupt database results before energy conversion? A finding requires proof that such values can enter `DailyExposureAverage` through current production code or persisted data. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q041 — Sample-count overflow

Can `usableDailyAverages.sumOf { it.sampleCount }` or weighted energy multiplication overflow its numeric type for realistic seven-day measurement volumes, and would overflow cause a null, NaN, or incorrect status? Establish actual types and maximum production rates before reporting. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q042 — Status threshold preservation

Do `NoiseLevel.NORMAL.maxDb` and `NoiseLevel.ELEVATED.maxDb` exactly preserve the prior SAFE/WARNING/DANGER boundary behavior, including equality at each threshold? Compare the removed Analytics implementation and tests; report a regression only with exact input and expected status evidence. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q043 — No-data state

Does `calculate` return `null` for empty or entirely unusable data so neither Trends nor Hearing displays a false SAFE status or misleading `0 dB` weekly average? Verify every caller's fallback fields, especially `AnalyticsViewModel`'s separate `weeklyAverageDb = ... ?: 0f` and `hasWeeklyExposureData`. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q044 — Today matching by day start

Is equality between `dailyAverage.dayStartMs` and `dayStartMs(nowMs, zoneId)` valid for the repository query's timezone and day-bucketing contract, including DST transitions, timezone changes, and stored timestamps generated under a previous device timezone? Prove whether mismatch is possible in current data production. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q045 — Multiple rows for today

Can `getDailyAveragesLast7Days()` produce more than one `DailyExposureAverage` for the same `dayStartMs`, and if so does `firstOrNull` make `todayVsWeekPercent` dependent on query ordering rather than aggregating all current-day data? Inspect the repository/DAO query and grouping guarantees before reporting. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q046 — Today-versus-week formula meaning

Is `((todayAverageDb - weeklyAverageDb) / weeklyAverageDb * 100).toInt()` the intended percent comparison for logarithmic decibel values, or should the UI communicate a dB delta or energy ratio instead? This is a defect only if an existing product/domain contract defines a different metric; otherwise record the scientific interpretation as a product question rather than inventing a code fix. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q047 — Percent truncation behavior

Is Kotlin `Float.toInt()` truncation toward zero intentional for positive and negative percentages, and do presentation strings remain accurate around values such as `3.85`, `-3.85`, and less than one percent? Compare `HearingHealthSummaryCalculatorTest` and `HearingHealthPresentationTest` before alleging an off-by-one error. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q048 — Missing current-day comparison

When weekly history exists but no row exists for the current local day, does nullable `todayVsWeekPercent` propagate through both full card and compact row without fabricated zero, stale comparison text, formatting exceptions, or blank layout gaps? Inspect all constructors, previews, and string formatting. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q049 — Zero and negative weekly average branch

Can a valid dataset produce `weeklyAverageDb <= 0f`, and if so is returning comparison `0` semantically correct, or should the comparison be absent? Establish production-domain validity and downstream meaning before treating the defensive branch as faulty. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q050 — Clock and timezone testability

Does `HearingViewModel.buildUiState` calling `System.currentTimeMillis()` and `ZoneId.systemDefault()` on every combined emission create nondeterminism, timezone inconsistency, or a stale day boundary because no flow emits at midnight? Determine whether the screen can remain open across midnight and show yesterday as “today,” and distinguish testability concerns from confirmed user-visible defects. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q051 — Shared calculator caller parity

Do Analytics and Hearing pass the same seven-day data, current time basis, and timezone to `HearingHealthSummaryCalculator`, ensuring identical output for the same moment? Trace both callers and any different collection timing that could cause visible disagreement between tabs. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q052 — Calculator regression coverage

Does `HearingHealthSummaryCalculatorTest.kt` cover empty input, zero-sample days, unequal sample counts, threshold boundaries, today absent, timezone/DST behavior, negative comparison, and rounding/truncation, with expectations derived from independent math rather than copying implementation? Missing cases are verification gaps unless they expose a proven defect. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

## E. Hearing ViewModel, entitlement, and Voice Baseline data flow

### Q053 — Flow lifecycle and sharing

Does `HearingViewModel`'s single `viewModelScope.launch { combine(...).collect { ... } }` lifecycle avoid duplicate collectors, leaks, unnecessary upstream work, and missed errors for the lifetime of the top-level Hearing destination? Compare with repository flow behavior and Hilt ViewModel scoping. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q054 — Initial state correctness

Can the default `HearingUiState()` briefly expose misleading Free/no-data UI while entitlement and repository flows are still loading, because the model has no explicit loading state? Verify actual first-emission timing and app startup gating before reporting a visible flicker or unauthorized UI state. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q055 — Combine error handling

If any of the five combined flows throws, does the entire Hearing state collector permanently stop without an error UI or retry, and can current repository implementations actually throw from these flows? Trace exception handling at repository boundaries and report only a reachable failure. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q056 — Pro downgrade redaction

When `isProUser` changes from true to false while Hearing is visible, are saved test, recovery, tinnitus, Voice Baseline, sound-detection, and Sleep states immediately redacted or locked in one atomic UI-state emission, with no live Pro data retained in remembered component state? Inspect `buildUiState` and child composables. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q057 — Pro upgrade restoration

When entitlement changes from Free to Pro, do the same repository flows re-emit or does the preferences emission alone rebuild state from the latest combined cached values so saved hearing data appears without reopening the screen? Confirm `combine` semantics and repository flow types. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q058 — Voice Baseline UI gate

Does `HearingUiState.canCalibrateVoiceBaseline` exactly enforce `isProUser && isRecording && soundDetectionEnabled`, and is that state used to disable or redirect every calibration action exposed by `VoiceBaselineCard`? Check whether a locked or disabled UI element can still invoke `onCalibrate` through semantics or nested clicks. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q059 — Voice Baseline execution gate race

Does `calibrateVoiceBaseline()` safely re-check current entitlement, `audioSessionManager.isRecording.value`, and effective sound-detection state at click time, and can any of those values change between the check and `captureVoiceBaseline(isProUser = true)` in a way that bypasses service-level policy or stores an invalid capture? Prove a reachable race and its consequence before reporting. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q060 — Voice Baseline capture argument

Is hardcoding `captureVoiceBaseline(isProUser = true)` safe because the caller has just checked effective entitlement, or does the service rely on the passed value in a way that can be stale or forged relative to the centralized entitlement policy? Inspect `AudioSessionManager.captureVoiceBaseline` and all callers. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q061 — Voice Baseline capture failure feedback

When `captureVoiceBaseline` returns `null` because there are too few samples, sound detection is unavailable, recording stops, or another precondition fails, does returning silently leave the user without necessary feedback compared with the previous Settings implementation? Establish prior behavior and current UI affordances before declaring this a regression. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q062 — Voice Baseline persistence failure

If `preferencesRepository.updateVoiceBaseline` throws or is cancelled, is the failure surfaced, retried, or safely ignored according to existing repository conventions, and could the card falsely show success from a transient local capture? Trace flow re-emission and prior implementation behavior. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q063 — Duplicate calibration submissions

Can rapid repeated taps launch concurrent `updateVoiceBaseline` writes with different captures, and if so is last-writer behavior intentional and safe? Check button enabled state, click throttling conventions, DataStore serialization, and whether a realistic harmful outcome exists. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q064 — Baseline metadata consistency

Does `buildUiState` prevent inconsistent combinations such as null `voiceBaselineLevelDb` with non-zero sample count or non-null captured time, and does `VoiceBaselineCard` render all persisted combinations defensively without claiming a baseline exists when its primary value is absent? Use actual preference defaults and migration behavior. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q065 — Locale-aware Voice Baseline formatting

Does `VoiceBaselineCard.voiceBaselineLabel` use `currentLocale` and a localized resource for decimals, units, and sample counts, avoiding `Locale.US`, raw concatenation, or grammatically invalid output in Finnish and English? Validate representative values and large-font wrapping. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q066 — Hearing ViewModel test realism

Do `HearingViewModelTest.kt` fakes model hot/cold flow behavior, entitlement changes, recording transitions, sound-detection changes, repository emissions, capture nullability, and persistence accurately enough to catch broken production wiring? Treat an unrealistic fake as a finding only when it can mask a concrete production defect; otherwise report a coverage gap. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

## F. Settings graph, shared ViewModel, and page ownership

### Q067 — One Settings ViewModel per graph entry

Does every Settings child route obtain the same `SettingsViewModel` through `navController.getBackStackEntry(Screen.Settings.route)` and `hiltViewModel(parentEntry)`, including after process restoration, direct navigation to a child, and legacy redirect? Determine whether a direct child route can exist without its parent graph entry and cause `getBackStackEntry` to fail. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q068 — Settings Home unnecessary ViewModel creation

`settingsRoutes` calls `settingsGraphViewModel(navController, backStackEntry)` on Home and discards the returned value; is this intentional eager initialization required for shared events/state, or unnecessary work with observable side effects? Inspect `SettingsViewModel.init`, billing collectors, backup state, and lifecycle expectations before reporting. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q069 — `remember(backStackEntry)` scoping

Is remembering the parent entry using only the child `backStackEntry` key sufficient when the `navController` or Settings graph entry changes, or can a stale parent entry survive graph recreation and scope the ViewModel incorrectly? Prove this under actual Compose/navigation lifecycle behavior rather than hypothetical recomposition. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q070 — Settings hub row completeness

Does `SettingsHomePage` expose exactly five rows—Calibration, Notifications & alerts, Data & privacy, Display, and Pro & About—with correct route constants, Material icons, localized labels, full-row click targets, and no obsolete emoji section headings? Verify resource values in English and Finnish. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q071 — Settings page ownership matrix

Does each old Settings section appear on exactly one intended page, with calibration and input on Calibration, noise notifications and passive monitoring on Notifications, Health Connect/export/backup/location/WAV/lockscreen privacy on Data & privacy, theme/features on Display, and purchase/About on Pro & About? Report duplicated or missing ownership only after tracing conditional content and Pro overlays. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q072 — Octave calibration child ownership

Does `settings/calibration/octave` correctly behave as a child of Calibration with shared state, proper Back behavior, locked preview for Free users, and no standalone top-level hub row that bypasses its context? Inspect `octaveCalibrationPresentation` and navigation wiring. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q073 — Shared state persistence across pages

When a user edits a setting on one page, navigates to another Settings page, and returns, does the graph-scoped ViewModel retain transient dialogs/messages and observe persisted state correctly without cross-page messages appearing in the wrong context? Exercise purchase, export, passive-monitoring, calibration, backup, and restore feedback. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q074 — Settings graph teardown

When leaving Settings for another top-level destination and later restoring the Settings stack, is the shared ViewModel lifetime consistent with saved back-stack state, and are collectors or pending one-shot events duplicated or replayed unexpectedly? Verify actual navigation ownership and event-flow semantics. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q075 — Back buttons and system Back parity

Do all Settings child pages use `SettingsPageScaffold` and `DbCheckTopAppBar` so the visible Back button and Android system Back produce the same destination, including Octave → Calibration and direct Pro & About navigation? Test both normal hub entry and upgrade entry stacks. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q076 — Top app bar semantics

Does the optional navigation icon in `DbCheckTopAppBar` have a localized content description, at least a 48 dp target, correct enabled semantics, and no invisible placeholder when `onBack` is null? Check every new Settings page and non-Settings caller affected by the shared component change. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q077 — Page scroll and nested scroll behavior

Can every Settings page reach all content at 360 × 800 dp and font scale 1.5 without nested-scroll conflicts, bottom-nav obstruction, clipped dialogs, or inaccessible final controls? Ground the answer in the actual scaffolds, modifiers, screenshot baselines, and if possible a semantics or UI test. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q078 — Settings source decomposition quality

Does moving the monolithic screen into `SettingsPages.kt` genuinely improve ownership, or has the new 844-line file retained coupled page logic, repeated actions, or private helpers with unclear lifecycle assumptions that create a concrete correctness or maintenance defect? Do not report file length alone as a bug. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

## G. Settings callbacks, permissions, privacy, and destructive actions

### Q079 — Microphone permission sequence for passive monitoring

In `SettingsNotificationsPage`, when microphone permission is initially missing and then granted, does the callback continue exactly once into notification-permission handling without prematurely starting passive monitoring or launching duplicate permission requests after recomposition? Trace `handleStartPassiveMonitoring` and launcher callbacks. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q080 — Microphone denial behavior

When microphone permission is denied temporarily or permanently, do `passiveMonitoringPermissionDenied`, `onPassiveMonitoringPermissionDenied`, the retry action, and app-settings action produce accurate UI without starting the service? Check lifecycle resume behavior after returning from system settings. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q081 — Notification permission callback semantics

The notification permission launcher calls `viewModel.startPassiveMonitoring()` regardless of the Boolean result; is that intentional because Android permits foreground-service start without `POST_NOTIFICATIONS`, and does the UI disclose the resulting notification behavior accurately? Report a defect only if current Android behavior, manifest/service requirements, or project policy proves the start is unsafe or misleading. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q082 — Pre-Android-13 notification path

Does `hasPostNotificationsPermission` plus `requestPostNotificationsPermissionIfNeeded` behave correctly on API levels where `POST_NOTIFICATIONS` does not exist as a runtime permission, ensuring passive monitoring starts immediately without a launcher that never returns? Inspect API guards and tests. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q083 — Passive-monitoring confirmation controller

Does `PassiveMonitoringStartConfirmationController` require the intended privacy confirmation exactly when enabling passive monitoring, survive recomposition/configuration changes correctly, prevent double confirmation, and clear pending state after accept or dismiss? Compare the controller test with its real composable integration. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q084 — Passive-monitoring active-session exclusion

Do the Settings UI and `SettingsViewModel.startPassiveMonitoring()` still prevent conflicts with an active manual measurement or other microphone owner, and is that protection enforced at execution time rather than only by disabled UI? Trace `AudioSessionManager` and service gates without changing their interfaces. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q085 — Notification schedule mixed selection

Do `NoiseNotificationsSection` and its state/action wiring preserve independently selected weekdays, including mixed selected/unselected chips, locale-correct day labels, time selection, and disabled schedule states? Use the current section test and screenshot rather than inferring from visual styling. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q086 — Warning and disclosure placement

Does `CompactDisclosureInfo` show the required microphone, background monitoring, raw WAV, location, Health Connect, lockscreen-public, export, backup, and destructive-action disclosures at the point where the user makes the relevant decision, with correct tone and readable semantics? Identify a missing disclosure only by tracing the actual sensitive action and project privacy contract. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q087 — Disclosure implementation accessibility

Does `CompactDisclosureInfo` provide sufficient color contrast, non-color cues, appropriate semantics, readable line wrapping, and non-clickable behavior unless an action is explicitly supplied? Validate dark/light themes and font scale 1.5. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q088 — Location permission state

In `SettingsDataPrivacyPage`, do `coarseLocationPermissionGranted`, `coarseLocationPermissionDenied`, the permission launcher, and `ON_RESUME` refresh correctly distinguish first request, temporary denial, permanent denial, and grant-from-settings without showing a stale warning or requesting excessive precision? Trace what the downstream feature actually needs. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q089 — Health Connect refresh and actions

Does refreshing Health Connect status on every `ON_RESUME` and wiring `healthSyncActions` preserve the previous permission, availability, Pro heart-rate overlay, and write-toggle behavior without duplicate launchers or stale results? Confirm each action reaches the same `SettingsViewModel` instance and service port. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q090 — CSV intent collection lifecycle

Does `LaunchedEffect(csvShareChooserTitle) { viewModel.csvExportIntents.collect { ... } }` collect exactly once per page instance, avoid replaying an old intent after navigation or configuration change, and correctly call either `onCsvShareStarted` or `onCsvShareUnavailable`? Inspect the event flow type and producer semantics. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q091 — Backup and restore safety wiring

Were all backup creation, restore selection, restore confirmation, measurement-exclusion, restart callback, and error-message paths preserved when `DataExportSection` moved to Data & privacy? Trace `onRestartAfterRestore` from `DbCheckNavHost` through the page and action object to `SettingsViewModel.confirmRestoreBackup`. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q092 — Clear-history destructive flow

Does the moved Clear history UI remain available to both Free and Pro users, require confirmation, block during active measurement, avoid affecting backups, and surface success/error through the correct page without accidental replay on return? Confirm execution gates and tests. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q093 — WAV preference and privacy

Does the moved WAV default toggle remain effectively off for Free users, avoid exposing or changing persisted Pro data through a locked preview, and show the raw-audio privacy warning before enablement? Trace `SettingsViewModel.updateWavRecordingDefaultEnabled` and UI-state mapping. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q094 — Lockscreen privacy controls

Do `LockscreenMeterSectionActions` correctly keep the general lockscreen meter setting distinct from “show publicly,” preserve Pro gates, and prevent public exposure from being enabled through stale state or a locked control? Inspect both ViewModel execution checks and UI semantics. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q095 — Pro purchase action lifecycle

Does `rememberProPurchaseAction(viewModel)` use the current Activity/context, survive recomposition safely, avoid retaining a destroyed Activity, and route `Completed`, `Cancelled`, `AlreadyOwned`, and `Failed` events to `SettingsPurchaseFeedback` on the correct page? Prove any lifecycle leak or lost event from code and flow behavior. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q096 — Timed message cross-page clearing

Can `TimedMessageEffect` or `SettingsDataPrivacyMessageEffects` clear a newer message because an older delay completes after state changes, or clear feedback belonging to another Settings page sharing the same ViewModel? Analyze effect keys, cancellation semantics, and message fields with a concrete sequence. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

## H. Meter layout, interaction, and retained functionality

### Q097 — Meter content order

Does `MeterReadoutContent` implement the required order—mode selector, session status, gauge, selected-mode summary or Live details, stats, Sound Reference—followed by fixed controls, with no removed feature silently relocated outside reach? Validate both dB Meter and Dosimeter states. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q098 — Fixed controls under constrained height

Does the outer weighted scroll column plus non-scrolling `MeterControlsSection` keep reset/start/share/camera controls visible and operable at 360 × 800 dp, smaller supported heights, landscape, IME/system inset changes, and font scale 1.5 without forcing the readout to an impossible zero height? Provide exact constraints for any failure. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q099 — “No default scrolling” acceptance criterion

At the specified 360 × 800 dp idle state, do the 200 dp compact gauge, spacing-token branches, Sound Reference collapsed row, top app bar, bottom navigation, and fixed controls all fit without scroll offset, while still allowing scroll when content expands? Validate the actual screenshot viewport and runtime density/insets rather than summing nominal dp values incompletely. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q100 — Large-font compact gauge

Is the `fontScale > 1f` threshold for choosing a 176 dp gauge appropriate and stable, or can minor user font scaling unexpectedly shrink the gauge while larger accessibility scales still clip other text? A defect requires a demonstrated layout or accessibility failure, not disagreement with the threshold. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q101 — Height-based compact policy

Does `maxHeight < 720.dp` measure the intended content viewport after scaffold insets, and can device cutouts, gesture navigation, landscape, or split-screen produce a wrong layout branch? Test boundary values near 720 dp and report only a concrete consequence. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q102 — Expandable state restoration

Do `rememberSaveable` states for Live details and Sound Reference restore safely across rotation/process recreation without making both expanded sections render controls off-screen or restoring expansion for a semantically different session/mode? Confirm expected product behavior and saved-state keys. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q103 — LiveActivityCard retained chart and waveform

Does collapsed/expanded `LiveActivityCard` preserve the existing live chart and waveform data, recording/idle states, content descriptions, and update performance, with no state collection removed when the UI moved behind an expander? Trace the ViewModel state and child components. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q104 — Live details header interaction

Is the entire `LiveDetailsHeader` a single accessible toggle with correct expanded/collapsed state semantics, at least 48 dp target, decorative chevron semantics, and no conflicting nested click targets? Validate TalkBack announcement and keyboard/switch access. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q105 — Sound Reference collapsed behavior

Does `SoundReferenceCollapsedRow` show a correct nearest reference and range for empty markers, out-of-range dB values, ties, and rapidly changing values, while remaining a single 48 dp accessible expander? Trace calculations back to `MeterUiState` rather than assuming display code owns them. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q106 — Sound Reference expanded geometry

Do `SoundReferenceRail`, marker positions, nearest highlighting, and range labels handle the complete supported dB domain without NaN, overlap, clipping, reversed order, or hardcoded geometry that conflicts with `ChartTokens` or theme spacing? Establish actual inputs and screenshot evidence. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q107 — Meter Sleep state removal

Were `sleepCardVisible` and all Sleep-only state/collector dependencies removed from `MeterUiState` and `MeterViewModel` without deleting the actual Sleep feature, its direct route, or the visibility preference used by Hearing? Search all callers and tests for stale Meter ownership. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q108 — Meter permissions unaffected

Did the UI restructure preserve microphone startup and Android 13 notification permission timing in `MeterScreen`, including the rule that startup asks only for missing microphone permission and recording start handles notification permission? Compare changed code and existing permission-policy tests. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q109 — Meter control enablement

Do `MeterControlsState.isShareEnabled`, `.isCameraOverlayEnabled`, recording state, and action wiring prevent invalid share/camera/reset operations without hiding legitimate Free behavior or bypassing execution gates? Trace each callback through `MeterScreenActions` to `MeterViewModel` or navigation. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q110 — Meter contract-test brittleness

Do `MeterScreenLayoutContractTest.kt` and `MeterExpandableComponentsContractTest.kt` verify layout and interaction meaningfully, or are they source-string checks that can pass despite a visually broken or inaccessible screen? Identify concrete missing runtime assertions as verification gaps unless an actual defect is demonstrated. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

## I. History layout, data semantics, and session-card accessibility

### Q111 — Empty History compactness

Does `HistoryUiState.Empty` render only the compact `EmptyState` with Meter CTA and no fake Last 24 Hours chart allocation, oversized skeleton, or unreachable content at 360 × 800 dp? Verify screenshot and composable branching. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q112 — Error-state message semantics

Does `HistoryError` using the raw error message as title and an empty description produce accessible, localized, and non-sensitive output, or can repository/internal exception text leak to the user? Prove the possible message source and actual UI output before reporting. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q113 — Last 24 Hours empty-versus-data split

Does `Last24HoursChart` choose `Last24HoursEmptyCard` only when the data contract says no meaningful samples exist, and avoid drawing invented axes/averages for empty or all-invalid input? Verify `last24HoursChartHeaderState` and caller-provided aggregates together. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q114 — Last 24 Hours time window and DST

Do X-axis labels, subtitle, chart positions, and accessibility description consistently use `windowStartMs` and `windowEndMs`, including DST days that are not exactly 24 clock hours and locale-specific time formatting? A finding requires a reproducible timestamp set. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q115 — Chart accessibility summary

Does `last24HoursChartContentDescription` communicate data availability, average, maximum, and trend without forcing TalkBack through decorative Canvas elements, and is the no-data card described accurately? Check duplicate semantics from visible text and parent merging. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q116 — SessionCard trailing statistics

Does the compact trailing `SessionCardStats` preserve peak and average meaning, correct labels/units, locale formatting, and readable contrast without squeezing the session name, tags, Sleep badge, or duration at 360 dp and font scale 1.5? Use the large-font screenshot baseline and semantics. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q117 — SessionCard click target interactions

Are the card-open action, emoji/edit action, Pro-lock action, and any nested controls unambiguous, at least 48 dp, and free from click propagation that opens Session Detail while the user intended to edit metadata? Test Free and Pro cards with and without custom names/tags. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q118 — Remembered editing session freshness

Can `editingSession` remembered in `HistorySuccessContent` become stale when the sessions list updates, causing a metadata save to overwrite newer name, emoji, or tags, or to edit a deleted session? Trace sheet arguments and repository update semantics; report only a reachable harmful sequence. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q119 — LazyColumn identity and state

Do `recentSessionItems` and other History list items use stable unique keys so inserts, deletions, metadata updates, and “view all” changes preserve the correct remembered/animated state and scroll position? Inspect the actual `LazyListScope` implementation and demonstrate any identity collision. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q120 — Automatic session naming localization

Do `hourOfDay`, `autoSessionName`, and `autoEmoji` use the intended timezone and localized strings, and can timezone changes or null/custom names result in inconsistent labels between History, Session Detail, exports, and persisted metadata? Separate pre-existing behavior from restructure regressions. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

## J. Shared components, design tokens, localization, and accessibility

### Q121 — Tertiary button casing

Does `DbCheckButton.kt` remove forced uppercase only from the tertiary style while preserving intended casing for primary/secondary styles and localized strings, and do any callers now depend on manual uppercase text that produces inconsistent UI? Compare component screenshots and all style callers. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q122 — Design-token compliance

Do the changed Meter, Trends, Hearing, History, Settings, and shared components use `DbCheckTheme.spacing`, radii, typography, card, chip, motion, and chart tokens wherever an equivalent token exists? Report hardcoded values only when a matching project token exists and the inconsistency has a concrete design-system or behavior impact. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q123 — Shared card and chip grammar

Do new surfaces extend `DbCheckCard`, `DbCheckChip`, `InlineStatusRow`, `DbCheckAlertDialog`, or other established shared components instead of rebuilding equivalent shape/color/padding behavior, and are any deviations intentional for navigation rows or charts? Do not force abstraction when semantics genuinely differ. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q124 — Pro locked-preview contract

Do all moved locked previews use the established `ProLockOverlay` grammar, keep rich preview content non-interactive beneath the overlay, use one upgrade action, and avoid exposing live Pro data? Check Hearing Test, Recovery, Tinnitus, Ambient, Voice Baseline, Sleep, octave calibration, Health Connect options, export, WAV, lockscreen, and display features. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q125 — Minimum target sizes

Do every new or changed row, icon button, chip, expander, toggle, card CTA, Back action, session edit action, and navigation item meet the 48 dp minimum interactive size under actual modifiers and parent constraints? Name the exact composable and measured/derived size for any violation. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q126 — TalkBack focus and roles

Are decorative icons and chevrons excluded from accessibility focus, clickable containers assigned suitable Button/Switch/Tab roles, selected and expanded states announced, and nested text not duplicated excessively? Review `HearingStatusRow`, Settings hub rows, Live details, Sound Reference, navigation items, and SessionCard specifically. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q127 — Color and non-color communication

Do SAFE/WARNING/DANGER, selected/unselected chips, locked states, success/error/info disclosures, chart series, and session statistics remain distinguishable without color alone and meet contrast in light/dark themes? Use token values or visual/accessibility tooling rather than subjective screenshots alone. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q128 — English and Finnish resource parity

Do all new string resources in `values/strings.xml` and `values-fi/strings.xml` have matching keys, valid format placeholders, correct grammatical meaning, and no untranslated user-visible hardcoded strings? Run or inspect localization tests and verify format-argument types at each call site. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q129 — Locale-sensitive numbers and times

Are calibration offsets, octave frequencies, Voice Baseline values, notification schedule times, hearing percentages, dB values, and History chart labels formatted with the active locale and correct unit resources, while machine-readable export formats remain untouched? Check for `String.format` defaults, concatenation, and locale capture during recomposition. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q130 — Font scale and text clipping

At font scales 1.0, 1.5, and a larger accessibility scale such as 2.0, do headings, bottom navigation labels, Settings rows, chips, stat columns, cards, buttons, and dialogs wrap or scroll without clipping, overlap, ellipsis that hides essential meaning, or inaccessible controls? Distinguish screenshot-covered 1.5 behavior from unverified larger scales. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q131 — Right-to-left resilience

Even though current resources are English and Finnish, do changed layouts use start/end alignment, directional icons, and logical padding so an RTL locale would not reverse navigation meaning or clip content? Report only concrete misuse of left/right APIs or non-mirroring icons, not absence of RTL screenshots alone. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q132 — Dynamic state announcements

When recording state, dB values, permission errors, purchase feedback, export status, passive-monitoring status, or Voice Baseline availability changes, are important changes announced appropriately without causing continuous TalkBack spam from high-frequency values? Inspect live-region semantics and update frequency. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

## K. Compose state, performance, concurrency, and lifecycle

### Q133 — Recomposition stability

Do the changed UI state classes, action containers, component parameters, and collections remain stable/skippable according to the regenerated debug and release Compose stability reports, or did the restructure introduce unstable parameters that cause high-frequency Meter or Analytics recomposition? Tie any finding to a specific report entry and caller update rate. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q134 — Action object allocation

Are `HearingScreenActions`, `MeterScreenActions`, `HistorySuccessActions`, `NoiseNotificationsSectionActions`, and other action containers recreated in a way that materially defeats Compose skipping or captures stale values? Measure or infer from stable function references and report only meaningful recomposition or correctness impact. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q135 — High-frequency flow isolation

Does adding Hearing and restructuring Meter/Analytics accidentally make broad screens recompose at audio sample frequency when only a gauge, chart, or live chip needs updates? Trace StateFlow aggregation and composable parameter boundaries; a finding needs an update-rate path and affected subtree. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q136 — Main-thread work

Do new presentation calculations, daily energy aggregation, profile formatting, chart geometry, screenshot-only data creation, or repository operations execute expensive work on the main thread during recomposition or flow collection? Quantify realistic workload before reporting a performance issue. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q137 — One-shot event collection

Do purchase, CSV share, backup/restore, clear-history, calibration, and passive-monitoring one-shot events have lifecycle-safe collection and consumption after Settings became a multi-page graph with one shared ViewModel? Look for lost events when the owning page is absent and replayed events when it returns. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q138 — Remembered local state ownership

Are local UI states such as expanders, permission-denied flags, editing session, dialogs, and selected page controls stored at the right level and keyed to the data they represent, or can state bleed between users, sessions, pages, or changed list items? Demonstrate an actual state mismatch. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q139 — Cancellation and navigation

If the user leaves a screen while calibration, export intent creation, purchase launch, permission request, or repository loading is pending, do callbacks target a valid lifecycle owner and avoid navigating, showing dialogs, or starting services twice on return? Trace coroutine and launcher ownership. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q140 — Process death restoration

After process death on a Settings child, Hearing screen, expanded Meter card, History edit sheet, or legacy upgrade redirect, can Navigation Compose, `rememberSaveable`, Hilt ViewModels, and SavedStateHandle restore to a valid state without missing graph parents or repeating destructive actions? Report unsupported cases as verification gaps unless a failure is reproduced or proven. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

## L. Tests, screenshot baselines, and verification quality

### Q141 — Unit-test execution result

Can the current merged tree reproduce a clean `:app:testDebugUnitTest` result, and do generated JUnit XML files confirm the claimed total and zero failures rather than relying on console summaries or old reports? If not rerun, mark the result unverified rather than inventing a defect. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q142 — Navigation behavioral coverage

Do tests cover bar and rail selection, five-item order, Settings child selected state, all fullscreen hidden-navigation routes, Session Detail History selection, reselection to root, stack restoration, legacy redirects, direct Pro route, and Hearing-flow returns? For every missing case, state whether it is merely missing coverage or exposes a confirmed bug. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q143 — Hearing ViewModel coverage

Do tests cover Free/Pro transitions, empty/data state, unequal-weight energy averages, latest-test redaction, all Recovery states, tinnitus redaction, Sleep visibility, Voice Baseline gating, null capture, successful persistence, and recording/sound-detection transitions? Do not equate an uncovered branch with an implementation defect. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q144 — Settings permission coverage

Do tests exercise microphone missing/granted/denied, notification missing/granted/denied, pre-Android-13 behavior, app-settings return, confirmation accept/dismiss, and active-recording conflicts for passive monitoring? Identify whether source-contract tests can actually catch callback-order regressions. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q145 — Settings page ownership tests

Can `SettingsScreenStructureTest`, `SettingsGraphContractTest`, and `SettingsReviewBehaviorTest` prove that every feature appears exactly once and its action reaches the correct ViewModel method, or do regex/source-string assertions create false confidence? Propose stronger tests only for material risks, and do not call weak tests a production bug by themselves. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q146 — Screenshot inventory integrity

Do `ComponentScreenshotTests.kt` and `FullScreenScreenshotTests.kt` currently contain 56 component and 39 full-screen `@PreviewTest` functions, with exactly 95 matching reference images and no orphaned, missing, or silently duplicated baseline names? Recount from the current tree instead of trusting documentation. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q147 — Required 360 × 800 matrix

Do full-screen references include light and dark 360 × 800 coverage for Meter idle/recording/Dosimeter, Trends Overview/Spectral/Environment, Hearing Free/Pro, History empty/sessions, Settings hub, and every Settings child including octave calibration? List any missing exact state as coverage, not automatically as a code defect. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q148 — Large-font screenshot matrix

Do the five `fontScale = 1.5f` previews target the highest-risk screens and states—Meter, Hearing, History, Data & privacy, and Notifications—and do they assert the intended 360 × 800 viewport? Determine whether untested Settings or Trends screens have source evidence of clipping before raising a finding. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q149 — Baseline review quality

Were changed screenshot baselines visually reviewed for content loss, clipping, selection ambiguity, theme contrast, unexpected scroll position, and missing controls rather than accepted solely because `updateDebugScreenshotTest` regenerated them? Name the exact image and visible defect for any finding. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q150 — Preview realism

Do preview states use realistic combinations of entitlement, feature flags, recording state, permissions, data availability, and long localized text, or can impossible state combinations make the screenshots pass while production fails? Tie any unrealistic fixture to the specific bug it could mask. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q151 — Screenshot contract-test robustness

Does `FullScreenScreenshotContractTest.kt` validate viewport, theme, state coverage, and preview/reference parity without depending on brittle source text that can be gamed by comments or dead preview functions? Separate a fragile test implementation from an actual missing screenshot artifact. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q152 — Documentation count synchronization

Does `UiDocumentationContractTest.kt` correctly derive and enforce screenshot counts and architecture phrases across `UI-SPEC.md`, `PROJECT.md`, `AGENTS.md`, and `memory/MEMORY.md`, or can it require stale exact wording while missing semantic contradictions? Report only a concrete current mismatch or harmful brittleness. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q153 — Static analysis verification

Can the current tree reproduce clean direct `:app:detekt` and `:app:ktlintCheck` results, and were both relevant source sets included? Follow project instructions by inspecting direct task evidence rather than running the user-owned `lc` wrapper unless explicitly authorized. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q154 — Build verification

Can `:app:assembleDebug` complete from the current merged tree with dependency verification and generated resources, proving route/resource/import changes compile outside isolated unit tests? If not executed, state the build as unverified rather than inferring breakage. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q155 — Device and instrumentation gap

Which critical behaviors still require emulator/device verification—navigation back stacks, TalkBack, permissions, purchase flow, Health Connect, backup/restore restart, passive monitoring foreground service, adaptive rail, and process death—and does any current static evidence already prove a defect? Keep unrun device scenarios in a separate verification-gap section. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q156 — Regression-test mutation resistance

For the most important contracts, would a plausible one-line mutation—wrong Hearing route, removed Pro gate, reversed permission callback, duplicated Settings ViewModel, null comparison converted to zero, hidden Sound Reference, or missing Back action—cause a test to fail for the right reason? Use targeted mutation reasoning; do not demand exhaustive mutation testing without a demonstrated risk. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

## M. Final integration, privacy, and release-readiness review

### Q157 — End-to-end Free user journey

Starting from a clean Free-user launch, can the user navigate through all five top-level destinations, understand locked Hearing and Settings features without seeing Pro data, start allowed Meter/History actions, reach the upgrade page, cancel purchase, and return without broken stacks or stale feedback? Record the exact first failing step if one exists. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q158 — End-to-end Pro Hearing journey

Can a Pro user start a measurement, enable sound detection, open Hearing, capture Voice Baseline, launch Hearing Test/Recovery/Tinnitus/Ambient/Sleep tools, complete or back out of each, and return to a correctly selected Hearing hub with fresh state? Distinguish unavailable device verification from a confirmed wiring failure. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q159 — End-to-end Settings data-safety journey

Can a user move among all Settings pages while changing preferences, requesting permissions, exporting CSV, creating/restoring backup, clearing history, and toggling privacy-sensitive features without actions being replayed, lost, or shown on the wrong page? Require explicit authorization before actually executing destructive tests against valuable user data. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q160 — Sensitive-data non-disclosure

Does the restructure avoid logging, screenshotting, previewing, or exposing saved hearing profiles, Voice Baseline values, location, Health Connect data, raw WAV status, backup paths, or session metadata to Free users or unauthorized external components? Search only changed paths first and distinguish pre-existing unrelated issues. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q161 — Entitlement single source of truth

Do all new UI gates consume effective `UserPreferences.isProUser` or established navigation/execution gates without introducing a second entitlement policy, debug/release divergence, or UI-only authorization? Trace every `isProUser` branch added by the restructure and verify protected actions remain server/service/domain gated where applicable. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q162 — External behavior preservation

Did the UI-only restructure leave manifest permissions, exported components, FileProvider paths, intent MIME types, Health Connect declarations, foreground-service types, database schema identity, backup compatibility, and export column formats unchanged? Report only diff-backed or behavior-backed changes attributable to this work. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q163 — Release documentation accuracy

Do `PROJECT.md`, `UI-SPEC.md`, `AGENTS.md`, and `memory/MEMORY.md` accurately state the five destinations, Hearing ownership, graph-scoped Settings ViewModel, nested routes, Voice Baseline gate and persistence path, screenshot totals, Meter/History behavior, and unchanged architecture boundaries? Cite exact contradictory lines if not. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**

### Q164 — Final objective verdict

After answering Q001–Q163, is there any confirmed defect that blocks release, any confirmed non-blocking defect, or only verification gaps and intentional decisions? Produce separate sections for confirmed findings, `No confirmed issue found` results, verification gaps, and optional product questions; do not convert uncertainty, preference, or AI-generated suspicion into mandatory code work. **Standalone review brief:** Review only the merged dBcheck UI restructure in `C:\Dev\dBcheck\.worktrees\codex-ui-rakenteen-uudistus` at commit `15d1c6a2486518f1a891404b6ad25d0cbf7b929c` (the same tree as merged `main` commit `05d68de0beb780f051b3312240fac1f80b2fa200`). Compare base `b5252aee5d45f17171a7ef2a6788e476f187e3b6` only when change-specific reasoning is needed. Inspect current production code, direct callers, relevant tests, and generated artifacts. Do not modify code. Confirm a defect only with exact `file:line`, evidence, impact, and a focused reproduction or test; otherwise answer `No confirmed issue found` or name a verification gap. AI reviewers can hallucinate, so do not invent a fix or force a change.**
